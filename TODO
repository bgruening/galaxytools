- fix help for trim galore wrapper
- bismark, PE read mapper is buggy, we wait for an patch to be applied to fix it properly
- documentation can be improved for many tools
- unit test are missing for many tools
- HOMER suite is WIP and currently just a proof of concept
- DESeq2 is currently only a proof of concept to demonstrate the how an R package can be wrapped
- infernal has only one tool wrapped, other tools are still missing
- statistical_hypothesis_testing under statistics can be enhanced with several other methods, documentation is missing
- unixtools, need to merge existing other useful tools into that repository, decision about the uniq wrapper, credits to Assaf Gordon
