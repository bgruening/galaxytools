<tool id="ctb_rdkit_rxn_smarts_filter" name="RXN Smarts Filter" version="@VERSION@.0">
    <description>ABCD</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code"><![CDATA[
        python3 '$__tool_directory__/rxn_smarts_filter.py'
        -i '$infile'
        -if '${infile.ext}'
        $multi
        $thin
        #if $fer
            --fer '$fer'
        #end if
        -o '$outfile'
        -of '${outfile.ext}'
        -l '$logfile'
    ]]></command>
    <inputs>
        <param name="infile" type="data" format="sdf" label="Input file" help="Input file in SDF format"/>
        <param name="multi" type="boolean" label="Multi Mode" truevalue="--multi" falsevalue="" help="Output one file for each reaction"/>
        <param name="thin" type="boolean" label="Thin Output Mode" truevalue="--thin" falsevalue=""/>
        <param name="fer" type="text" value="" label="Molecule to compare against"/>
    </inputs>    
    <outputs>
        <data name="logfile" format="txt" label="Logfile for ${tool.name}"/>
        <data name="outfile" format="sdf" />
        <collection name="output_list" type="list">
            <discover_datasets pattern="(?P&lt;designation&gt;.+)\.sdf$" ext="sdf" />
        </collection>
    </outputs>
    <tests>
    </tests>
    <help><![CDATA[
        ABCCDEFGHIJKLMNOPQRSTUVWXYZ
    ]]></help>
    <expand macro="citations" />
</tool>