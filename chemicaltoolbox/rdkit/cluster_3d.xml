<tool id="ctb_rdkit_cluster_3d" name="Cluster 3D" version="@VERSION@.0">
    <description>ABCD</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code"><![CDATA[
        python3 '$__tool_directory__/cluster_3d.py'
        -i '$infile'
        -if '${infile.ext}'
        -o '$outfile'
        -of '${outfile.ext}'
        -l '$logfile'
    ]]></command>
    <inputs>
        <param name="infile" type="data" format="sdf" label="Input file" help="Input file in SDF format"/>
    </inputs>    
    <outputs>
        <data name="logfile" format="txt" label="Logfile for ${tool.name}"/>
        <data name="outfile" format="sdf"/>
    </outputs>
    <tests>
        <test>
            <param name="infile" value="sulfonyl_chloride.sdf" ftype="sdf"/>
            <output name="logfile">
                <assert_contents>
                    <has_line_matching expression="Number processed: \d+ New conformer count: \d+"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
        ABCCDEFGHIJKLMNOPQRSTUVWXYZ
    ]]></help>
    <expand macro="citations"/>
</tool>