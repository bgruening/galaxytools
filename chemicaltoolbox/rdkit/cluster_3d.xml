<tool id="ctb_rdkit_cluster_3d" name="Cluster 3D" version="@TOOL_VERSION@+galaxy@GALAXY_VERSION@">
    <description>using RDKit</description>
    <macros>
        <import>macros.xml</import>
        <token name="@GALAXY_VERSION@">0</token>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code"><![CDATA[
        cluster_3d
        -i '$infile'
        -if sdf
        --meta
        -o outp
        -of tsv &>> $logfile &&
        cat outp_metrics.txt &>> $logfile &&
        gzip -d outp.tsv.gz
    ]]></command>
    <inputs>
        <param name="infile" type="data" format="sdf" label="Input file" help="Input file in SDF format"/>
    </inputs>    

    <expand macro="outputs" output_format="tsv" filetype="tabular"/>
    
    <tests>
        <test>
            <param name="infile" value="sulfonyl_chloride.sdf" ftype="sdf"/>
            <output name="logfile">
                <assert_contents>
                    <has_line_matching expression="Number processed: \d+ New conformer count: \d+"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
        ABCCDEFGHIJKLMNOPQRSTUVWXYZ
    ]]></help>
    <expand macro="citations"/>
</tool>