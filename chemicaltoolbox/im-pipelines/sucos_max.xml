<tool id="ctb_rdkit_sucos" name="SuCOSMax" version="@TOOL_VERSION@+galaxy@GALAXY_VERSION@">
    <description>usinf RDKit</description>
    <macros>
        <import>macros.xml</import>
        <token name="@GALAXY_VERSION@">0</token>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code"><![CDATA[
        ln -s '$target' ./target.${target.ext} &&
        sucos_max
        -i '$infile'
        -if '${infile.ext}'
        --target-molecules './target.${target.ext}'
        --meta
        -o outp
        -of tsv &>> $logfile &&
        cat outp_metrics.txt &>> $logfile &&
        gzip -d outp.tsv.gz
    ]]></command>
    <inputs>
        <param name="infile" type="data" format="sdf" label="Input file" help="Input file in SDF format"/>
        <param name="target" type="data" format="sdf" label="Target file" help="Target molecules to compare against"/>
    </inputs>

    <expand macro="outputs" output_format="tsv" filetype="tabular"/>
    
    <tests>
        <test>
            <param name="infile" value="poses.sdf" ftype="sdf"/>
            <param name="target" value="hits.sdf" ftype="sdf"/>
            <output name="logfile">
                <assert_contents>
                    <has_line_matching expression="Completed \d+\ comparisons"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
        ABCCDEFGHIJKLMNOPQRSTUVWXYZ
    ]]></help>
    <expand macro="citations" />
</tool>