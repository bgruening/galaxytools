<tool id="bgchem_fragment_merger" name="Merging" version="0.1">
    <description>fragmented molecules</description>
    <requirements>
        <requirement type="package" version="0.1">cheminfolib</requirement>
    </requirements>
    <command interpreter='python'>
    merger.py 
        -i $infile 
        -o $outfile 
        --molwt-cutoff $molwt_cutoff
        $mdid
        --repeats $repeats

        2>&#38;1
    </command>
    <inputs>
        <param name="infile" type="data" format="sdf,smi,mol,inchi,cml" label="Input file" help="Dataset missing? See TIP below"/>
        <param name="molwt_cutoff" type="integer" value="500" label="The number of positions between windows to report."/>

        <param name="mdid" type="boolean" truevalue="" falsevalue="--mdid-off" checked="True" label="Molecule dependent iteration depth" help="" />
        <param name="repeats" type="integer" value="2" label="Number of repeats" help="Number of repeats all new created fragments should be merged against the inital ones.">
            <validator type="in_range" min="1" />
        </param>

    </inputs>
    <outputs>
        <data format="smi" name="outfile" label="Fragment merging on ${on_string}"/>
    </outputs>
    <tests>
        <test>
        </test>
    </tests>
<help>

**Note**. Only fragments with marked sticky ends can be merged. To get such a file check the appropriate option in the fragmenter tool.

**What it does**
Merging small molecules together to larger compounds using some predifined reactions.

-----

**Input**

A molecule file with marked molecules. You can create such a file with the Fragmenter tool and the option 'mark atoms'.

**Output**

A molecule file with new created/merged molecules.

With the option *Molecule dependend iteration depth* and *Number of repeats* you can adjust the created number of compounds and the actual computing time.



 </help>

</tool>
