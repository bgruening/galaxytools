<tool id="gff3_to_gb_embl" name="GFF3 converter" version="0.1">
    <description>to GenBank or embl</description>
    <command interpreter="python">
        gff_to_gb_embl.py --fasta $ifile_fasta --gff $ifile_gff --oformat $oformat --outfile $outfile
    </command>
    <inputs>
        <param name="ifile_fasta" type="data" format="fasta" label="Input sequence file" help="Dataset missing? See TIP below"/>
        <param name="ifile_gff" type="data" format="gff,gff3" label="Input annotation file, in gff format" help="Dataset missing? See TIP below"/>

        <param name='oformat' type='select' format='text' label="Output format.">
            <option value='embl'>embl</option>
            <option value='genbank'>GenBank</option>
        </param>
    </inputs>
    <outputs>
        <data name="outfile" format="embl">
            <change_format>
               <when input="oformat" value="genbank" format="genbank"/>
            </change_format>
        </data>
    </outputs>
    <tests>
        <test>
            <param name="input" value="streptomyces_coelicolor.dna" />
            <output name="output" file="fasta_tool_convert_from_dna.out" />
        </test>
    </tests>
    <help>

**What it does**

This tool calculates the reverse strand of a given Sequence.

-----

    </help>
</tool>
