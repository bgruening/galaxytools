<tool id="3dtrees_potree" name="3Dtrees: Potree Converter" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="24.2">
    <description>Convert LAS/LAZ point clouds to Potree octree format for web visualization</description>
    <macros>
        <token name="@TOOL_VERSION@">1.2.1</token>
        <token name="@VERSION_SUFFIX@">0</token>
    </macros>
    <requirements>
        <container type="docker">ghcr.io/3dtrees-earth/3dtrees_potree:@TOOL_VERSION@</container>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
        mkdir -p data &&
        #for $f in $input
            ln -s '$f' data/'${f.element_identifier}.laz' &&
        #end for
        python /src/run.py
        --source data
        --outdir output
        --encoding '$encoding'
        --method '$method'
        --chunk-method '$chunk_method'
        #if $attributes and str($special_coloring.enabled) != 'true'
            --attributes '$attributes'
        #end if
        #if $grid_size and int($grid_size) > 0
            --grid-size '$grid_size'
        #end if
        --numthreads \${GALAXY_SLOTS:-$numthreads}
        $keep_chunks
        $no_chunking
        $no_indexing
        #if str($special_coloring.enabled) == 'true'
            --special-coloring
            --special-coloring-palette '$special_coloring.palette'
            --special-coloring-n-colors '$special_coloring.n_colors'
            --special-coloring-n-neighbors '$special_coloring.n_neighbors'
            --special-coloring-instance-attributes '$special_coloring.instance_attributes'
            --special-coloring-ground-id '$special_coloring.ground_id'
            --special-coloring-ground-color '$special_coloring.ground_color'
            $special_coloring.sidecar_json
        #end if
        $generate_page
        #if $title
            --title '$title'
        #end if
        #if $generate_page
            &&

            ## Setup HTML with extra files for Galaxy viewing
            cp output/potree-vis.html '$viewer_html' &&
            mkdir -p '$viewer_html.files_path' &&
            cp -r /opt/PotreeConverter/resources/page_template/libs '$viewer_html.files_path/' &&
            cp -r output/pointclouds '$viewer_html.files_path/' &&
            chmod -R a+rX '$viewer_html.files_path'
        #end if
        2>&1
        ]]>
    </command>
    <inputs>
        <param name="input" type="data" format="laz" multiple="true" label="Point Cloud Files" help="Input LAS/LAZ files to convert to Potree format" />
        <param argument="--generate-page" type="boolean" truevalue="-p potree-vis" falsevalue="" checked="false" label="Generate interactive Viewer" help="Generate a ready-to-use HTML viewer page for in-Galaxy visualization" />
        <param argument="--encoding" type="select" label="Encoding" help="Compression encoding for output">
            <option value="BROTLI" selected="true">Brotli</option>
            <option value="UNCOMPRESSED">Uncompressed</option>
        </param>
        <param argument="--method" type="select" label="Sampling Method" help="Point sampling method for octree generation">
            <option value="poisson" selected="true">Poisson</option>
            <option value="poisson_average">Poisson Average</option>
            <option value="random">Random</option>
        </param>
        <param argument="--chunk-method" type="select" label="Chunk Method" help="Chunking method used during the conversion phase">
            <option value="LASZIP" selected="true">LASZIP</option>
        </param>
        <param argument="--attributes" type="text" optional="true" label="Attributes" help="Comma-separated list of attributes to include in output (e.g., 'rgb,intensity,classification'). Leave empty to use defaults. Ignored when Special Coloring is enabled." >
             <validator type="regex" message="Only letters, numbers, underscores, and commas are allowed">^[a-zA-Z0-9_,]+$</validator>
        </param>
        <param argument="--grid-size" type="integer" optional="true" value="0" min="0" label="Grid Size" help="Grid size for chunking phase. Leave at 0 to use PotreeConverter's automatic size selection." />
        <param argument="--keep-chunks" type="boolean" truevalue="--keep-chunks" falsevalue="" checked="false" label="Keep Chunks" help="Skip deleting temporary chunks during conversion" />
        <param argument="--no-chunking" type="boolean" truevalue="--no-chunking" falsevalue="" checked="false" label="No Chunking" help="Disable chunking phase" />
        <param argument="--no-indexing" type="boolean" truevalue="--no-indexing" falsevalue="" checked="false" label="No Indexing" help="Disable indexing phase" />
        <conditional name="special_coloring">
            <param name="enabled" argument="--special-coloring" type="select" label="Special Coloring" help="Add neighbor-aware coloring dimensions for one or more instance attributes.">
                <option value="false" selected="true">Disabled</option>
                <option value="true">Enabled</option>
            </param>
            <when value="true">
                <param name="palette" argument="--special-coloring-palette" type="text" value="candy" label="Special Coloring Palette" help="Built-in palette name, or comma-separated #RRGGBB colors used to generate coloring_id values">
                    <validator type="regex" message="Use a built-in palette name or comma-separated hex colors like #ff0000,#00ff00">^(sky|sea|cozy|fairy|winter|rainbow|pastel|candy|boring|#?[0-9A-Fa-f]{6}(,#?[0-9A-Fa-f]{6})*)$</validator>
                </param>
                <param name="n_colors" argument="--special-coloring-n-colors" type="integer" value="10" min="1" label="Special Coloring Color Count" help="Number of non-ground coloring IDs to generate" />
                <param name="n_neighbors" argument="--special-coloring-n-neighbors" type="integer" value="10" min="1" label="Special Coloring Neighbors" help="Nearest instance centroids considered when avoiding nearby color collisions" />
                <param name="instance_attributes" argument="--special-coloring-instance-attributes" type="text" value="PredInstance_SAT,PredInstance_FoMa" label="Special Coloring Instance Attributes" help="Comma-separated point attributes containing instance IDs. PredInstance_FoMa also accepts inputs named PredInstance_FM.">
                    <validator type="regex" message="Only letters, numbers, underscores, and commas are allowed">^[A-Za-z0-9_,]+$</validator>
                </param>
                <param name="ground_id" argument="--special-coloring-ground-id" type="integer" value="0" label="Special Coloring Ground Instance ID" help="Instance ID treated as ground/background" />
                <param name="ground_color" argument="--special-coloring-ground-color" type="text" value="#808080" label="Special Coloring Ground Color" help="Ground/background color as #RRGGBB">
                    <validator type="regex" message="Use a hex color like #ffffff">^#[0-9A-Fa-f]{6}$</validator>
                </param>
                <param name="sidecar_json" argument="--special-coloring-sidecar-json" type="boolean" truevalue="--special-coloring-sidecar-json" falsevalue="" checked="false" label="Write Special Coloring JSON" help="Write special_coloring_mapping.json and patch generated Potree HTML viewers to use those colors." />
            </when>
            <when value="false" />
        </conditional>
        <param argument="--title" type="text" optional="true" label="Viewer Title" help="Optional page title for the generated Potree viewer." >
            <validator type="regex" message="Only letters, numbers, spaces, dots, commas, colons, underscores, and dashes are allowed">^[A-Za-z0-9 .,_:-]+$</validator>
        </param>
        <param name="numthreads" type="hidden" value="10" />
    </inputs>
    <outputs>
        <!-- HTML viewer for in-Galaxy viewing (when generate_page is specified) -->
        <data name="viewer_html" format="html" label="Potree Visualization">
            <filter>generate_page</filter>
        </data>
        <!-- Collection output - discovers files in output directory -->
        <collection name="potree_output" type="list" label="Potree Output">
            <discover_datasets pattern="(?P&lt;designation&gt;metadata)\.json$" directory="output" format="json" recurse="true"/>
            <discover_datasets pattern="(?P&lt;designation&gt;special_coloring_mapping[^/]*?)\.json$" directory="output" format="json"/>
            <discover_datasets pattern="(?P&lt;designation&gt;hierarchy)\.bin$" directory="output" format="binary" recurse="true"/>
            <discover_datasets pattern="(?P&lt;designation&gt;octree)\.bin$" directory="output" format="binary" recurse="true"/>
            <discover_datasets pattern="(?P&lt;designation&gt;log)\.txt$" directory="output" format="txt" recurse="true"/>
        </collection>
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <param name="input" value="mikro.laz" ftype="laz"/>
            <param name="generate_page" value="true"/>
            <!-- HTML viewer for Galaxy -->
            <output name="viewer_html" ftype="html">
                <assert_contents>
                    <has_text text='Potree.loadPointCloud("./pointclouds/potree-vis/metadata.json", "potree-vis"'/>
                </assert_contents>
            </output>
            <!-- Collection output for octree data -->
            <output_collection name="potree_output" type="list" count="4">
                <element name="metadata" ftype="json">
                    <assert_contents>
                        <has_json_property_with_value property="points" value="12917" />
                    </assert_contents>
                </element>
                <element name="hierarchy" ftype="binary">
                    <assert_contents>
                        <has_size value="2500" delta="500"/>
                    </assert_contents>
                </element>
                <element name="octree" ftype="binary">
                    <assert_contents>
                        <has_size value="135000" delta="50000"/>
                    </assert_contents>
                </element>
                <element name="log" ftype="txt">
                    <assert_contents>
                        <has_text text="counting mikro.laz"/>
                    </assert_contents>
                </element>
            </output_collection>
        </test>
        <test expect_num_outputs="1">
            <param name="input" value="mikro_multi_segmented.laz" ftype="laz"/>
            <param name="special_coloring|enabled" value="true"/>
            <param name="special_coloring|palette" value="#ff0000,#00ff00"/>
            <param name="special_coloring|sidecar_json" value="true"/>
            <output_collection name="potree_output" type="list" count="6">
                <element name="metadata" ftype="json">
                    <assert_contents>
                        <has_json_property_with_value property="points" value="12917" />
                        <has_text text="PredInstance_SAT" />
                        <has_text text="PredInstance_FM" />
                        <has_text text="species_id_sat" />
                        <has_text text="species_id_foma" />
                        <has_text text="coloring_id_sat" />
                        <has_text text="coloring_id_fm" />
                    </assert_contents>
                </element>
                <element name="special_coloring_mapping_coloring_id_fm" ftype="json">
                    <assert_contents>
                        <has_json_property_with_value property="version" value="1" />
                        <has_json_property_with_value property="color_attribute" value='"coloring_id_fm"' />
                        <has_json_property_with_value property="instance_attribute" value='"PredInstance_FM"' />
                        <has_json_property_with_value property="ground_instance_id" value="0" />
                        <has_json_property_with_value property="ground_coloring_id" value="0" />
                        <has_json_property_with_value property="ground_color" value='"#808080"' />
                        <has_json_property_with_value property="palette" value='"#ff0000,#00ff00"' />
                        <has_json_property_with_value property="n_colors" value="10" />
                        <has_json_property_with_value property="n_neighbors" value="10" />
                    </assert_contents>
                </element>
                <element name="special_coloring_mapping_coloring_id_sat" ftype="json">
                    <assert_contents>
                        <has_json_property_with_value property="version" value="1" />
                        <has_json_property_with_value property="color_attribute" value='"coloring_id_sat"' />
                        <has_json_property_with_value property="instance_attribute" value='"PredInstance_SAT"' />
                        <has_json_property_with_value property="ground_instance_id" value="0" />
                        <has_json_property_with_value property="ground_coloring_id" value="0" />
                        <has_json_property_with_value property="ground_color" value='"#808080"' />
                        <has_json_property_with_value property="palette" value='"#ff0000,#00ff00"' />
                        <has_json_property_with_value property="n_colors" value="10" />
                        <has_json_property_with_value property="n_neighbors" value="10" />
                        <has_json_property_with_value property="0" value='{"hex": "#808080", "rgb": [128, 128, 128]}' />
                        <has_json_property_with_value property="1" value='{"hex": "#ff0000", "rgb": [255, 0, 0]}' />
                        <has_json_property_with_value property="10" value='{"hex": "#00ff00", "rgb": [0, 255, 0]}' />
                        <has_json_property_with_value property="0" value="0" />
                    </assert_contents>
                </element>
            </output_collection>
        </test>
    </tests>
    <help>

**What it does**

This tool converts LAS/LAZ point cloud files to the Potree octree format for efficient web-based visualization using the Potree viewer.

**Key features:**

1. **Octree Level-of-Detail (LOD)**: Creates an efficient hierarchical structure for progressive rendering
2. **Multiple compression options**: Supports BROTLI compression for smaller file sizes
3. **Flexible sampling methods**: Choose between Poisson, Poisson Average, or Random sampling
4. **Web-ready output**: Generates files that can be directly served for Potree viewer visualization
5. **Optional web page generation**: Can generate a ready-to-use HTML viewer page
6. **Special instance coloring**: Optionally adds display-only coloring attributes from one or more instance attributes while preserving original RGB

-----

**Input**

- **Point Cloud Files**: Input LAS/LAZ files to convert
- **Encoding**: Compression type (BROTLI for compression, UNCOMPRESSED for raw data, DEFAULT)
- **Sampling Method**: Algorithm for point sampling during octree generation (Poisson, Poisson Average, Random)
- **Attributes**: Optional comma-separated list of point attributes to include in output
- **Chunk Method**: Chunking backend used during conversion
- **Grid Size**: Optional chunking grid size override; use 0 for automatic selection
- **Keep Chunks**: Preserve temporary chunk files after conversion
- **No Chunking**: Disable the chunking phase
- **No Indexing**: Disable the indexing phase
- **Special Coloring**: Add point attributes for neighbor-aware instance colors.
- **Special Coloring Palette**: Built-in palette name (sky, sea, cozy, fairy, winter, rainbow, pastel, candy, boring) or custom comma-separated ``#RRGGBB`` colors
- **Special Coloring Color Count**: Number of non-ground coloring IDs
- **Special Coloring Neighbors**: Number of nearest instance centroids considered when avoiding nearby color reuse
- **Special Coloring Instance Attributes**: Comma-separated point attributes containing instance IDs, default ``PredInstance_SAT,PredInstance_FoMa``
- **Special Coloring Ground Instance ID**: Instance ID treated as ground/background, default ``0``. Negative instance IDs are also treated as background.
- **Special Coloring Ground Color**: Ground/background color as ``#RRGGBB``
- **Write Special Coloring JSON**: Write ``special_coloring_mapping*.json`` files and patch generated Potree HTML viewers to use the first sidecar color mapping.
- **Generate Page**: Create a web viewer page with the specified name
- **Viewer Title**: Optional title shown in the generated viewer page

-----

**Output**

The tool produces a collection containing:

- **metadata** (JSON): Metadata file describing the octree structure and point cloud properties
- **hierarchy** (binary): Binary file containing the octree hierarchy
- **octree** (binary): Binary file containing the point data
- **log** (text): Conversion log file
- **special_coloring_mapping*** (JSON, optional): Mapping from coloring IDs to colors and from each instance attribute to coloring IDs when Write Special Coloring JSON is enabled

When **Generate Page** is specified (e.g., "viewer"):

- **{name} Potree Viewer** (HTML): Interactive viewer that can be viewed directly in Galaxy
  - Note: This tool is meant for visualization - other tools (eg. measurements, camera) may not work properly
  - Includes libs/ and pointclouds/ as extra files

- **{name} Potree Viewer Package (Download)** (ZIP): Self-contained package for local use
  - Download, extract, and open files in PotreeViewer.

-----

**Workflow Integration**

This tool converts point cloud data to an optimized web visualization format using PotreeConverter's octree algorithm.
The output can be downloaded and visualized using the Potree viewer.

    </help>
    <creator>
        <organization name="3Dtrees-Team, University of Freiburg" url="https://github.com/3dTrees-earth"/>
    </creator>
    <citations>
        <citation type="bibtex">@misc{potreeconverter, title = {PotreeConverter: Create multi-resolution octree structure}, author = {Markus Schütz}, year = {2024}, url = {https://github.com/potree/PotreeConverter}}</citation>
        <citation type="bibtex">
            @misc{3dtrees_potree, title = {3Dtrees: Potree Converter}, author = {3Dtrees-Project}, year = {2025}}
        </citation>
    </citations>
</tool>
