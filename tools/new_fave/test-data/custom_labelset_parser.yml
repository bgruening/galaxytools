# A distinctive parsed stress value must reach every measurement row.
parser: custom_test
properties:
  - name: stress
    updates: stress
    default: custom_stress
    rules:
      - rule: all_labels
        conditions:
          - attribute: label
            relation: rematches
            set: ".*"
        return: custom_stress
