<tool id="lissero" name="LisSero" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@" license="GPL-3.0">
    <description>In silico serogroup typing prediction for Listeria monocytogenes</description>
    <macros>
        <token name="@TOOL_VERSION@">0.4.10</token>
        <token name="@VERSION_SUFFIX@">0</token>
        <token name="@PROFILE@">23.0</token>
    </macros>
    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">lissero</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
        #set input_name = str($input.name)
        lissero
        #if $settings.advanced == "advanced"
          --min_id $settings.min_id
          --min_cov $settings.min_cov
        #end if
        '$input' > output &&
        awk -v id="$input_name" 'BEGIN{OFS="\t"} NR==1 {print; next} {$1=id; print}' output > tmp && 
        mv tmp output
    ]]></command>
    <inputs>
        <param type="data" name="input" format="fasta,fasta.gz"/>
        <conditional name="settings">
            <param name="advanced" type="select" label="Specify advanced parameters">
                <option value="simple" selected="true">No, use program defaults.</option>
                <option value="advanced">Yes, see full parameter list.</option>
            </param>
            <when value="simple">
            </when>
            <when value="advanced">
                <param name="min_id" type="float" label="Minimum percent identity to accept a match" value="95.0" min="0" max="100"/>
                <param name="min_cov" type="float" label="Minimum coverage of a gene to accept a match" value="95.0" min="0" max="100"/>
            </when>
        </conditional>
    </inputs>
    <outputs>
        <data format="tabular" name="lissero_tsv" label="${tool.name} on ${on_string}: LisSero" from_work_dir="output"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input" value="NC_002973_subset.fna" ftype="fasta"/>
            <output name="lissero_tsv" file="NC_002973_subset_results.txt"/>
        </test>
    </tests>
    <help><![CDATA[
**What it does**

LisSero predicts the serotype of *Listeria monocytogenes*.

The input should be an assembled genome in FASTA format. The output is a tabular file containing the predicted serotype and associated matching information.

-----

**Input**

- Genome assembly in FASTA format (optionally gzip-compressed).

-----

**Output**

A tabular file containing the predicted serotype for the input genome.

-----

**Parameters**

Advanced parameters are optional.

- **Minimum percent identity to accept a match**
    - Minimum sequence identity (%) required for a marker to be considered present.
    - Default: **95.0**

- **Minimum coverage of a gene to accept a match**
    - Minimum percentage of the reference marker that must be covered by the alignment.
    - Default: **95.0**
    ]]></help>
    <citations>
        <citation type="doi">10.1128/JCM.42.8.3819-3822.2004</citation>
    </citations>
</tool>
