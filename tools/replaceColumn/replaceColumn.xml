<tool id="replace_column_script" name="Replace columns" version="0.1">
    <description>Replace the values of a column by values which are defined in a convert file.</description>
    
    <command>
        <![CDATA[
        python "$replaceColumnScript" '$inputs'
        ]]>
    </command>
    <configfiles>
        <inputs name="inputs" />
        <configfile name="replaceColumnScript">
            <![CDATA[
import sys

replace_file = "$replace_information"
original_file = "$original_file"
column = int("$column_replace") - 1 ## assume that user thinks column 0 is column 1
ignore_start_lines = int("$skip_lines")
delimiter_local = "\t" if str("$delimiter") == "" else str("$delimiter")
print ":::", delimiter_local, ":::"
if column < 0:
    print('No negative column id possible! Columns IDs have the minimal value of \'1\'')
    sys.exit(1)

## read conversion information to index 
conversion = {}

with open(replace_file, 'r') as conversion_file:
    for line in conversion_file:
        conv_key_value = line.split()
        if len(conv_key_value) == 2:
            conversion[conv_key_value[0].strip('\n')] = conv_key_value[1].strip('\n')
        else:
            conversion[conv_key_value[0].strip('\n')] = ''
                    

## read file line by line, search for column entry if it can be replaced. Otherwise it will be skipped.
with open("output_file", 'w') as output:

    with open(original_file) as original:

        for i, line in enumerate(original):
            write = True

            if i < ignore_start_lines:
                output.write(line)
                continue

            if str("$delimiter") == "":
                line_content = line.split()
            else:
                line_content = line.split(str("$delimiter"))
                
            string_out = ""
            for j, line_content_column in enumerate(line_content):
                if j == column:
                    if conversion[line_content_column] != '':
                        string_out += conversion[line_content_column]
                        string_out += delimiter_local
                    else:
                        write = False
                        break
                else:
                    string_out += line_content_column
                    if j + 1 < len(line_content):
                        string_out += delimiter_local
            
            string_out += "\n"

            if write:
                output.write(string_out)

]]>
        </configfile>
    </configfiles>
    <inputs>
        <param name="replace_information" type="data" format="txt" value='' label="Replace information file" help="This file contains in the first column the entries that should be replaced by the values of the second column." />
        <param name="original_file" type="data" format="txt" value='' label="File where a column should be replaced" help="This file should be processed. A the entries of a specific column is replaced by the information given of 'Replace information file'" />
        <param name="column_replace" type="data_column" data_ref="original_file" multiple="false" label="Which column should be replaced?" />
        <param name="skip_lines" type='integer' value='0' label="Skip n starting lines" />
        <param name="delimiter" type="select" label="Delimited by">
            <option value="" selected="True">Tab</option>
            <option value=" ">Whitespace</option>
            <option value=".">Dot</option>
            <option value=",">Comma</option>
            <option value="-">Dash</option>
            <option value="_">Underscore</option>
            <option value="|">Pipe</option>
        </param>
    </inputs>
    <outputs>
        <data  name="outfile_replace" format="txt" from_work_dir="output_file"/>
    </outputs>
    <tests>
        <test>
            <param name="replace_information" value="GRCh38_ensembl2UCSC.txt"/>
            <param name="original_file" value="original_file"/>
            <param name="column_replace" value="1"/>
            <param name="skip_lines" value="1"/>
            <param name="delimiter" value="" />
            <output name="outfile_replace" file="result_file"/>
        </test>
    </tests>
    <help>
        <![CDATA[
**What it does**

This tool replaces the entries of a defined column with entries given by a replacement file. 
E.g.: The replacement file holds the information of the naming scheme of ensembl annotated chromosomes in the frist column and in the second the UCSC annotation. 
A file which is having information about chromosomes in ensembl notation in column x can now be converted to a file which holds the same information but in UCSC annotation.
        ]]>
    </help>
</tool>