<tool id="rnazIndex" name="rnazIndex.pl" version="0.1.0">
    <requirements>
        <requirement type="package">rnaz</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
    rnazIndex.pl
	$outformat
	$info
	'$input'
	> '$output'
    ]]></command>
    <inputs>
        <param type="data" name="input" format="text" />
		<param name="outformat" type="select" label="Which output format" >
			<option value="--gff">Generate GFF formatted output</option>
            <option value="--bed" selected="true">Generate BED formatted output</option>
		</param>
		<param name="info" type="select" label="Which type of information">
            <option value="--loci" selected="true"> Use the locus information to generate the lines for the GFF and BED files. This is the default.</option>
            <option value="--windows">Print the "windows" and not the "loci". Probably, rarely used function.</option>
        </param>
		
    </inputs>
    <outputs>
        <data name="output" format="text" />
    </outputs>
    <tests>
        <test>
            <param name="input" value="tRNA.rnaz"/>
            <output name="output" file="tRNA.rna.bed"/>
        </test>
        <test>
            <param name="input" value="tRNA.rnaz"/>
			<param name="--gff" value="tRNA.rnaz"/>
            <output name="output" file="tRNA.rna.gff"/>
        </test>		
    </tests>
    <help><![CDATA[
    Usage:
    rnazIndex.pl [options] [file]

	Options: 
	-g, --gff Generate GFF formatted output.

    -b, --bed Generate BED formatted output.

    --ucsc In UCSC MAF alignment files it is common to use sequence
    identifiers like for example hg17.chr22. However, in BED are
    usually specific for a given assembly and therefore only chr22 is
    used in the BED files. With this option you change any identifier
    of the form X.Y into Y. Moreover, the scores are multiplied by
    1000 and rounded to integers since the UCSC genome browser expects
    scores between 0 and 1000.

    -l, --loci Use the locus information to generate the lines for the
    GFF and BED files. This is the default.

    -w, --windows Print the windows and not the loci. Probably, rarely
    used function.  

	]]></help>

	<citations>
        <citation type="doi">10.1142/9789814295291_0009</citation>
    </citations>    

</tool>
