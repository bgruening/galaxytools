<tool id="viennarna_rnapaln" name="RNApaln" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>RNA alignment based on sequence base pairing propensities</description>
    <macros>
        <token name="@EXECUTABLE@">RNApaln</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    RNApaln < '$input' > '$output'
    --mode=$alignmentmode
    -T$temperature -d$dangling
    $backtrack
    --gapo=$gapo
    --gape=$gape
    --seqw=$seqw
    $endgaps
    #if $varExists('$advancedOptions.nolp')
        $advancedOptions.noconversion
        $advancedOptions.nolp
        $advancedOptions.nogu
        $advancedOptions.noclosinggu
        $advancedOptions.notetra
    #end if
    #if $backtrack and str($alignmentmode) == "m"
        && cat backtrack.file >> '$output'
    #end if
]]>
    </command>
    <inputs>
        <param format="fasta" name="input" type="data" label="FASTA file"/>
        <param name="temperature" type="float" value="37.0" label="Temperature [°C]" help="-T"/>
        <param name="dangling" type="select" label="How to treat dangling end energies" help="-d">
            <option value="0">0: ignore dangling ends</option>
            <option value="1">1: unpaired bases participate in one dangling end only</option>
            <option value="2" selected="true">2: unpaired bases participate in all dangling ends</option>
            <option value="3">3: allow coaxial stacking</option>
        </param>
        <param argument="--mode" name="alignmentmode" type="select" label="Comparison Option">
            <option value="p" selected="true">p: pairwise (1st with 2nd, 3rd with 4th, ...)</option>
            <option value="m">m: matrix (each with each, output in matrix form)</option>
            <option value="f">f: first (1st with 2nd, 1st with 3rd, ...)</option>
            <option value="c">c: continuous (1st with 2nd, 2nd with 3rd, ...)</option>
        </param>
        <param argument="-B" name="backtrack" type="boolean" checked="true" truevalue="-B" falsevalue="" label="Print an alignment"/>
        <param argument="--gapo" name="gapo" type="float" value="1.5" label="Gap open penalty"/>
        <param argument="--gape" name="gape" type="float" value="0.67" label="Gap extension penalty"/>
        <param argument="--seqw" name="seqw" type="float" value="0.5" label="Weight of sequence compared to structure"/>
        <param argument="--endgaps" name="endgaps" type="boolean" checked="false" truevalue="--endgaps" falsevalue="" label="Allow free end-gaps"/>
        <conditional name="advancedOptions">
            <param name="advancedSelector" type="select" label="Advanced options">
                <option value="basic">Basic options</option>
                <option value="advanced">Advanced options</option>
            </param>
            <when value="advanced">
                <param argument="--noLP" name="nolp" type="boolean" truevalue="" falsevalue="--noLP" checked="true" label="Allow lonely base-pairs"/>
                <param argument="--noGU" name="nogu" type="boolean" truevalue="" falsevalue="--noGU" checked="true" label="Allow GU pairing"/>
                <param argument="--noClosingGU" name="noclosinggu" type="boolean" truevalue="" falsevalue="--noClosingGU" checked="true" label="Allow GU pairing at the ends"/>
                <param argument="--noTetra" name="notetra" type="boolean" truevalue="" falsevalue="--noTetra" checked="true" label="Allow stabilization for loops, hairpins etc."/>
                <param argument="--noconv" name="noconversion" type="boolean" truevalue="" falsevalue="--noconv" checked="true" label="Convert Thymine to Uracil (T -> U)"/>
            </when>
            <when value="basic"/>
        </conditional>
    </inputs>
    <outputs>
        <data format="txt" name="output" label="${tool.name} on ${on_string}"/>
        <collection name="matrix_outputs" type="list" label="${tool.name} dot plots on ${on_string}">
            <discover_datasets pattern="(?P&lt;designation&gt;.+)_dp\.ps" ext="rna_eps" visible="true"/>
        </collection>
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <param name="input" value="rnapaln_input1.fa"/>
            <output name="output" file="rnapaln_result1.txt"/>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNApaln**

RNA alignment based on sequence base pairing propensities.

Uses string-alignment techniques to perform fast pairwise structural alignments of RNAs. Similar to RNApdist, secondary structure is incorporated in an approximate manner by computing base pair probabilities, which are then reduced to a vector holding the probability that a base is paired upstream, downstream, or remains unpaired. Such pair propensity vectors can then be compared using standard alignment algorithms. In contrast to RNApdist, RNApaln performs similarity (instead of distance) alignments, considers both sequence and structure information, and uses affine (rather than linear) gap costs.

-----

**Input format**

RNApaln requires one FASTA file.

-----

**Outputs**

- Alignment scores and alignments (if -B option is selected)
- Dot plot matrices in PostScript format
]]>
    </help>
    <expand macro="citations"/>
</tool>
