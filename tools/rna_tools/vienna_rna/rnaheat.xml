<tool id="viennarna_rnaheat" name="RNAheat" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Calculate energies of RNA in a temperature range</description>
    <macros>
        <token name="@EXECUTABLE@">RNAheat</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    RNAheat < '$input' > '$output'
    --Tmin $tmin --Tmax $tmax -d$dangling --stepsize=$stepsize --ipoints=$ipoints
    #if $varExists('$advancedOptions.noconversion')
        $advancedOptions.noconversion
        $advancedOptions.nolp
        $advancedOptions.nogu
        $advancedOptions.noclosinggu
        $advancedOptions.notetra
        #if $advancedOptions.nsp
            --nsp='$advancedOptions.nsp'
        #end if
    #end if
]]>
    </command>
    <inputs>
        <param format="fasta" name="input" type="data" label="Input FASTA file"/>
        <param name="tmin" type="float" value="0.0" label="Minimal temperature [°C]" help="--Tmin"/>
        <param name="tmax" type="float" value="100.0" label="Maximal temperature [°C]" help="--Tmax"/>
        <param name="stepsize" type="float" value="1.0" label="Temperature step size [°C]" help="--stepsize"/>
        <param name="ipoints" type="integer" value="2" label="Data points to fit parabola to: 2*value+1" help="--ipoints"/>
        <param name="dangling" type="select" label="How to treat dangling end energies" help="-d">
            <option value="0">0: ignore dangling ends</option>
            <option value="1">1: unpaired bases participate in one dangling end only</option>
            <option value="2" selected="true">2: unpaired bases participate in all dangling ends</option>
            <option value="3">3: allow coaxial stacking</option>
        </param>
        <conditional name="advancedOptions">
            <param name="advancedSelector" type="select" label="Advanced options">
                <option value="basic">Basic options</option>
                <option value="advanced">Advanced options</option>
            </param>
            <when value="advanced">
                <param argument="--noLP" name="nolp" type="boolean" truevalue="" falsevalue="--noLP" checked="true" label="Allow lonely base-pairs"/>
                <param argument="--noGU" name="nogu" type="boolean" truevalue="" falsevalue="--noGU" checked="true" label="Allow GU pairing"/>
                <param argument="--noClosingGU" name="noclosinggu" type="boolean" truevalue="" falsevalue="--noClosingGU" checked="true" label="Allow GU pairing at the ends"/>
                <param argument="--noTetra" name="notetra" type="boolean" truevalue="" falsevalue="--noTetra" checked="true" label="Allow stabilization for loops, hairpins etc."/>
                <param argument="--noconv" name="noconversion" type="boolean" truevalue="" falsevalue="--noconv" checked="true" label="Convert Thymine to Uracil (T -> U)"/>
                <param argument="--nsp" type="text" value="" label="Allow non-standard pairs in addition to AU, GC, and GU pairs."
                    help="Comma-separated list of additionally allowed pairs."/>
            </when>
            <when value="basic"/>
        </conditional>
    </inputs>
    <outputs>
        <data format="txt" name="output" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input" value="rnaheat_input1.fa"/>
            <output name="output" file="rnaheat_result1.txt"/>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNAheat**

Calculates the specific heat of RNA sequences in a temperature range by numeric differentiation of the partition function. A parabola is fit to 2*ipoints+1 data points to calculate the 2nd derivative. Increasing the ipoints parameter produces a smoother curve.

-----

**Input format**

RNAheat requires one FASTA file.

-----

**Outputs**

A list of pairs: temperature (°C) and specific heat (kcal/(mol*K)) for each sequence in the input file.
]]>
    </help>
    <expand macro="citations"/>
</tool>
