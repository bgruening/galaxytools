<tool id="viennarna_rna2dfold" name="RNA2Dfold" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Explore structure space between two reference structures</description>
    <macros>
        <token name="@EXECUTABLE@">RNA2Dfold</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
        RNA2Dfold -T $temperature -d $dangling --stochBT=$nrbt
        #if int($nk) > -1 and int($nl) > -1
            --neighborhood=$nk:$nl
        #end if
        #if $varExists('$pfselect.pfscaling')
            --partfunc --pfScale=$pfselect.pfscaling
        #end if
        $noBT
        $advancedOptions.noconversion
        $advancedOptions.noclosinggu
        $advancedOptions.notetra
        $advancedOptions.circ
        #if $varExists('$advancedOptions.maxK')
            --maxDist1=$advancedOptions.maxK
            --maxDist2=$advancedOptions.maxL
        #end if
        < '$custom_input' > '$out_file'
]]>
    </command>
    <inputs>
        <param format="txt,dbn" name="custom_input" type="data" label="Custom input file"/>
        <param name="temperature" type="float" value="37.0" label="Temperature [°C]" help="-T"/>
        <param name="nrbt" type="integer" value="1" label="Number of Boltzmann backtrack samples" help="--stochBT"/>
        <param name="nk" type="integer" value="-1" label="[k]: Distance to first reference structure for neighborhood backtracking" help="--neighborhood=k:l"/>
        <param name="nl" type="integer" value="-1" label="[l]: Distance to second reference structure for neighborhood backtracking" help="--neighborhood=k:l"/>
        <param name="dangling" type="select" label="How to treat dangling end energies" help="-d">
            <option value="2" selected="true">Unpaired bases participate in all dangling ends (2)</option>
            <option value="0">Ignore dangling ends (0)</option>
        </param>
        <param argument="--noBT" name="noBT" type="boolean" checked="true" truevalue="" falsevalue="--noBT"
            label="Backtrack structures (don't calculate energy contributions only)"/>
        <conditional name="pfselect">
            <param name="pf" type="select" label="Calculate partition function" help="--partfunc">
                <option value="no">no</option>
                <option value="yes">yes</option>
            </param>
            <when value="yes">
                <param name="pfscaling" type="float" value="1.0" label="Scaling factor for the partition function"/>
            </when>
            <when value="no"/>
        </conditional>
        <section name="advancedOptions" title="Advanced options">
            <param argument="--noClosingGU" name="noclosinggu" type="boolean" truevalue="" falsevalue="--noClosingGU" checked="true" label="Allow GU pairing at the ends"/>
            <param argument="--noconv" name="noconversion" type="boolean" truevalue="" falsevalue="--noconv" checked="true" label="Convert Thymine to Uracil (T -> U)"/>
            <param argument="--noTetra" name="notetra" type="boolean" truevalue="" falsevalue="--noTetra" checked="true" label="Allow stabilization for loops, hairpins etc."/>
            <param argument="--circ" type="boolean" truevalue="--circ" falsevalue="" checked="false" label="Model as circular RNA structure"/>
            <param argument="--maxDist1" name="maxK" type="integer" value="10" max="44" label="Maximum distance to first reference structure"/>
            <param argument="--maxDist2" name="maxL" type="integer" value="10" max="43" label="Maximum distance to second reference structure"/>
        </section>
    </inputs>
    <outputs>
        <data format="dbn" name="out_file" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="custom_input" value="rna2dfold_input1.txt"/>
            <param name="nrbt" value="0"/>
            <output name="out_file" file="rna2dfold_result1.txt"/>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNA2Dfold**

Partitions the secondary structure space into (base pair) distance classes according to two fixed reference structures. It expects a sequence and two secondary structures in dot-bracket notation as input. For each distance class, the MFE representative, Boltzmann probabilities and Gibbs free energy is computed. Additionally, a stochastic backtracking routine produces samples of representative suboptimal secondary structures from each partition.

-----

**Input format**

RNA2Dfold requires one input file in the following format:

- 1st line: RNA sequence
- 2nd line: first reference structure in dot-bracket format
- 3rd line: second reference structure in dot-bracket format

-----

**Outputs**

- Text output with secondary structures and energies for each distance class
]]>
    </help>
    <expand macro="citations"/>
</tool>
