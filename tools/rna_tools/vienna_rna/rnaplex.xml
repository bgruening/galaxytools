<tool id="viennarna_rnaplex" name="RNAplex" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Find targets of a query RNA</description>
    <macros>
        <token name="@EXECUTABLE@">RNAplex</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    RNAplex
    #if str($clustalOption.clustalFlag) == "clustal"
        --alignment-mode
        -q'$clustalOption.input1' -t'$clustalOption.input2'
    #else
        < '$clustalOption.input'
    #end if
    -T$temperature
    --interaction-length=$maxLength
    --extension-cost=$extensionCost
    --scale-accessibility=$scaleAccessibility
    $constraint
    #if $varExists('clustalOption.meltingOption.probeConc')
        --probe-mode
        --probe-concentration=$clustalOption.meltingOption.probeConc
        --na-concentration=$clustalOption.meltingOption.naConc
        --mg-concentration=$clustalOption.meltingOption.mgConc
        --k-concentration=$clustalOption.meltingOption.kConc
        --tris-concentration=$clustalOption.meltingOption.trisConc
    #end if
    #if $varExists('$advancedOptions.duplexDistance')
        --fast-folding=$advancedOptions.fastFolding
        --duplex-distance=$advancedOptions.duplexDistance
        --energy-threshold=$advancedOptions.enThreshold
    #end if
    > '$output'
]]>
    </command>
    <inputs>
        <conditional name="clustalOption">
            <param name="clustalFlag" type="select" label="Input options">
                <option value="fasta" selected="true">FASTA input file</option>
                <option value="clustal">ClustalW input files</option>
            </param>
            <when value="fasta">
                <param format="fasta" name="input" type="data" label="FASTA file"/>
                <conditional name="meltingOption">
                    <param name="meltingSelector" type="select" label="Calculate melting temperature">
                        <option value="no" selected="true">No</option>
                        <option value="yes">Yes</option>
                    </param>
                    <when value="yes">
                        <param argument="--probe-concentration" name="probeConc" type="float" value="0.1" label="Concentration of the probe"/>
                        <param argument="--na-concentration" name="naConc" type="float" value="1.0" label="Na concentration"/>
                        <param argument="--mg-concentration" name="mgConc" type="float" value="1.0" label="Mg concentration"/>
                        <param argument="--k-concentration" name="kConc" type="float" value="1.0" label="K concentration"/>
                        <param argument="--tris-concentration" name="trisConc" type="float" value="1.0" label="Tris concentration"/>
                    </when>
                    <when value="no"/>
                </conditional>
            </when>
            <when value="clustal">
                <param format="txt" name="input1" type="data" label="Clustal file / Query sequences"/>
                <param format="txt" name="input2" type="data" label="Clustal file / Target sequences"/>
            </when>
        </conditional>
        <param name="temperature" type="float" value="37.0" label="Temperature [°C]" help="-T"/>
        <param argument="--interaction-length" name="maxLength" type="integer" min="0" value="40" label="Maximal interaction length"/>
        <param argument="--extension-cost" name="extensionCost" type="integer" value="0" label="Extension cost"/>
        <param argument="--scale-accessibility" name="scaleAccessibility" type="float" value="1.0" label="Scale all opening energies by a factor"/>
        <param argument="--constraint" type="boolean" truevalue="--constraint" falsevalue="" checked="false" label="Calculate structures subject to constraints"/>
        <conditional name="advancedOptions">
            <param name="advancedSelector" type="select" label="Advanced options">
                <option value="basic">Basic options</option>
                <option value="advanced">Advanced options</option>
            </param>
            <when value="advanced">
                <param argument="--fast-folding" name="fastFolding" type="select" label="Backtracking">
                    <option value="0" selected="true">Based on standard energy model (0) - normal</option>
                    <option value="2">Based on approximate plex model (2) - faster</option>
                    <option value="1">No recomputation (1) - fastest</option>
                </param>
                <param argument="--duplex-distance" name="duplexDistance" type="integer" value="0" label="Distance between target 3' ends of two consecutive duplexes"/>
                <param argument="--energy-threshold" name="enThreshold" type="float" value="-100000" label="Minimum energy gain for a duplex [kcal/mol]"/>
            </when>
            <when value="basic"/>
        </conditional>
    </inputs>
    <outputs>
        <data format="txt" name="output" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <conditional name="clustalOption">
                <param name="clustalFlag" value="fasta"/>
                <param name="input" value="rnaplex_input1.fa"/>
            </conditional>
            <output name="output" file="rnaplex_result1.txt"/>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNAplex**

Computes optimal and suboptimal secondary structures for the hybridization of two RNA sequences. The calculation is simplified by allowing only inter-molecular base pairs.

-----

**Input format**

RNAplex requires either one FASTA file or two ClustalW files as input.

-----

**Outputs**

The computed optimal and suboptimal structures are given, one structure per line. Each line consists of: the structure in dot-bracket format with a "&" separating the two strands; the range of the structure in the two sequences in the format "from,to : from,to"; and the energy of duplex structure in kcal/mol.
]]>
    </help>
    <expand macro="citations"/>
</tool>
