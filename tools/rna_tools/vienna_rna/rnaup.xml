<tool id="viennarna_rnaup" name="RNAup" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Calculate the thermodynamics of RNA-RNA interactions</description>
    <macros>
        <token name="@EXECUTABLE@">RNAup</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    RNAup < '$input' > '$output'
    -T$temperature
    --dangles=$dangling
    $constraint
    --ulength=$ulength
    --contributions=#echo ''.join(str($contributions).split(','))#
    --window=$window
    $includeboth
    $interactionpairwise
    $interactionfirst
    --extend5=$extendfive
    --extend3=$extendthree
    #if $varExists('$advancedOptions.nolp')
        --pfScale=$advancedOptions.pfscale
        $advancedOptions.noconversion
        $advancedOptions.nolp
        $advancedOptions.nogu
        $advancedOptions.noclosinggu
        $advancedOptions.notetra
    #end if
    && tar -cf '$accesibilitiesFile' *.out
]]>
    </command>
    <inputs>
        <param format="fasta" name="input" type="data" label="FASTA file"/>
        <param name="temperature" type="float" value="37.0" label="Temperature [°C]" help="-T"/>
        <param name="dangling" type="select" label="How to treat dangling end energies" help="-d">
            <option value="2" selected="true">unpaired bases participate in all dangling ends (2)</option>
            <option value="0">ignore dangling ends (0)</option>
            <option value="1">unpaired bases participate in one dangling end only (1)</option>
            <option value="3">allow coaxial stacking (3)</option>
        </param>
        <param argument="--constraint" name="constraint" type="boolean" truevalue="--constraint" falsevalue="" checked="false" label="Constraints for the secondary structure"/>
        <param argument="--ulength" name="ulength" type="integer" min="0" value="4" label="Length of the unstructured region"/>
        <param argument="--contributions" name="contributions" type="select" multiple="true" label="Contributions to the probability of being unpaired">
            <option value="S" selected="true">S: sum of all</option>
            <option value="H">H: unpaired within hairpin loop</option>
            <option value="I">I: unpaired within interior loop</option>
            <option value="M">M: unpaired within multiloop</option>
            <option value="E">E: unpaired within exterior loop</option>
            <validator type="no_options" message="Please select at least one contribution."/>
        </param>
        <param argument="--window" name="window" type="integer" min="0" value="25" label="Maximal length of the region of interaction"/>
        <param argument="--include_both" name="includeboth" type="boolean" truevalue="--include_both" falsevalue="" checked="false" label="Include the probability of unpaired regions in both RNAs"/>
        <param argument="--interaction_pairwise" name="interactionpairwise" type="boolean" truevalue="--interaction_pairwise" falsevalue="" checked="false" label="Activate pairwise interaction mode"/>
        <param argument="--interaction_first" name="interactionfirst" type="boolean" truevalue="--interaction_first" falsevalue="" checked="false" label="Activate pairwise interaction mode, compare always with the first sequence"/>
        <param argument="--extend5" name="extendfive" type="integer" min="0" value="0" label="Extend the region of interaction on the 5' side"/>
        <param argument="--extend3" name="extendthree" type="integer" min="0" value="0" label="Extend the region of interaction on the 3' side"/>
        <conditional name="advancedOptions">
            <param name="advancedSelector" type="select" label="Advanced options">
                <option value="basic">Basic options</option>
                <option value="advanced">Advanced options</option>
            </param>
            <when value="advanced">
                <param argument="--pfScale" name="pfscale" type="float" value="1.07" label="Use scale*mfe as an estimate for the ensemble free energy"/>
                <param argument="--noLP" name="nolp" type="boolean" truevalue="" falsevalue="--noLP" checked="true" label="Allow lonely base-pairs"/>
                <param argument="--noGU" name="nogu" type="boolean" truevalue="" falsevalue="--noGU" checked="true" label="Allow GU pairing"/>
                <param argument="--noClosingGU" name="noclosinggu" type="boolean" truevalue="" falsevalue="--noClosingGU" checked="true" label="Allow GU pairing at the ends"/>
                <param argument="--noTetra" name="notetra" type="boolean" truevalue="" falsevalue="--noTetra" checked="true" label="Allow stabilization for loops, hairpins etc."/>
                <param argument="--noconv" name="noconversion" type="boolean" truevalue="" falsevalue="--noconv" checked="true" label="Convert Thymine to Uracil (T -> U)"/>
            </when>
            <when value="basic"/>
        </conditional>
    </inputs>
    <outputs>
        <data format="txt" name="output" label="${tool.name} on ${on_string}"/>
        <data format="tar" name="accesibilitiesFile" label="${tool.name} accessibilities on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <param name="input" value="rnaup_input1.fa"/>
            <output name="output" file="rnaup_result1.txt"/>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNAup**

Calculates the thermodynamics of RNA-RNA interactions by decomposing the binding into two stages. (1) First the probability that a potential binding site remains unpaired (equivalent to the free energy needed to open the site) is computed. (2) Then this accessibility is combined with the interaction energy to obtain the total binding energy. All calculations are done by computing partition functions over all possible conformations.

-----

**Input format**

RNAup requires one FASTA file. For interaction mode, concatenate query and target sequences with an '&', or use the --interaction-pairwise or --interaction-first flags.

-----

**Outputs**

- Most accessible region of each sequence
- Accessibility profiles in a tar file
]]>
    </help>
    <expand macro="citations"/>
</tool>
