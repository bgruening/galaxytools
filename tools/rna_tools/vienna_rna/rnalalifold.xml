<tool id="viennarna_rnalalifold" name="RNALalifold" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Calculate locally stable secondary structures for a set of aligned RNAs</description>
    <macros>
        <token name="@EXECUTABLE@">RNALalifold</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    RNALalifold '$input' > '$output'
    -T$temperature -d$dangling
    -L$span
    $mis
    #if $varExists('$advancedOptions.nolp')
        $advancedOptions.nolp
        $advancedOptions.nogu
        $advancedOptions.noclosinggu
        $advancedOptions.notetra
        --cfactor=$advancedOptions.cfactor
        --nfactor=$advancedOptions.nfactor
        $advancedOptions.ribosum
    #end if
]]>
    </command>
    <inputs>
        <param format="txt" name="input" type="data" label="Clustal alignment file"/>
        <param name="span" type="integer" min="0" max="5000" value="60" label="Base pair span" help="Maximal distance between two paired bases (-L)"/>
        <param name="temperature" type="float" value="37.0" label="Temperature [°C]" help="-T"/>
        <param name="dangling" type="select" label="How to treat dangling end energies" help="-d">
            <option value="0">0: ignore dangling ends</option>
            <option value="1">1: unpaired bases participate in one dangling end only</option>
            <option value="2" selected="true">2: unpaired bases participate in all dangling ends</option>
            <option value="3">3: allow coaxial stacking</option>
        </param>
        <param argument="--mis" name="mis" type="boolean" checked="false" truevalue="--mis" falsevalue="" label="Most Informative Sequence output"/>
        <conditional name="advancedOptions">
            <param name="advancedSelector" type="select" label="Advanced options">
                <option value="basic">Basic options</option>
                <option value="advanced">Advanced options</option>
            </param>
            <when value="advanced">
                <param argument="--noLP" name="nolp" type="boolean" truevalue="" falsevalue="--noLP" checked="true" label="Allow lonely base-pairs"/>
                <param argument="--noGU" name="nogu" type="boolean" truevalue="" falsevalue="--noGU" checked="true" label="Allow GU pairing"/>
                <param argument="--noClosingGU" name="noclosinggu" type="boolean" truevalue="" falsevalue="--noClosingGU" checked="true" label="Allow GU pairing at the ends"/>
                <param argument="--noTetra" name="notetra" type="boolean" truevalue="" falsevalue="--noTetra" checked="true" label="Allow stabilization for loops, hairpins etc."/>
                <param argument="--cfactor" name="cfactor" type="float" value="1.0" label="Weight of the covariance term"/>
                <param argument="--nfactor" name="nfactor" type="float" value="1.0" label="Penalty for non-compatible sequences in the covariance term"/>
                <param argument="--ribosum_scoring" name="ribosum" type="boolean" truevalue="--ribosum_scoring" falsevalue="" checked="false" label="Use ribosum scoring matrix"/>
            </when>
            <when value="basic"/>
        </conditional>
    </inputs>
    <outputs>
        <data format="txt" name="output" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input" value="rnalalifold_input1.clustal"/>
            <output name="output" file="rnalalifold_result1.txt"/>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNALalifold**

Calculates locally stable RNA secondary structures with a maximal base pair span for a set of aligned RNAs. For a sequence of length n and a base pair span of L the algorithm uses only O(n+L*L) memory and O(n*L*L) CPU time. Thus it is practical to scan very large genomes for short RNA structures.

-----

**Input format**

RNALalifold requires one Clustal alignment file.

-----

**Outputs**

- Energy of the consensus structures in the alignment
]]>
    </help>
    <expand macro="citations"/>
</tool>
