<tool id="viennarna_kinwalker" name="Kinwalker" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Simulate cotranscriptional folding of RNAs</description>
    <macros>
        <token name="@EXECUTABLE@">kinwalker</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    head -n 1 '$input_sequence' | head -c -1 -q  > seq.ident
    &&
    sed '1d' '$input_sequence' > input.seq
    &&
    kinwalker
        $init_structure
        $interrupt
        --barrier_heuristic $barrier_heuristic.used
        #if $barrier_heuristic.used == "M"
            --grouping $barrier_heuristic.grouping
            --lookahead $barrier_heuristic.lookahead
        #else if $barrier_heuristic.used == "B"
            --maxkeep $barrier_heuristic.maxkeep
        #end if
        --dangle $dangle
        --nolonely $noLonelyPairs
        --transcribed $transcribed
        --transcription_rate $transcription_rate
        --windowsize $windowsize
        < input.seq
        > blah
    &&
    sed -n '2s/[\.\(\)]\+\s\+\(.\+\)$/ mfe: \1/p' blah > energy
    &&
    cat seq.ident energy > '$mfe_struct'
    &&
    sed -e '2s/^\([\.\(\)]\+\).*$/\1/' -ne '1,2p' blah >> '$mfe_struct'
    &&
    sed -e 's/[ \t]*$//' -ne '/TRAJ/,/Kinwal/ {/TRAJ/n;/Kinwal/!{s/\s/\t/gp}}' blah > '$trajectory'
]]>
    </command>
    <inputs>
        <param name="input_sequence" format="fasta" type="data" label="Input sequence" help="A single sequence in FASTA format"/>
        <param argument="--init_structure" name="init_structure" type="boolean" truevalue="--init_structure" falsevalue="" checked="false" label="Start with a structure other than the open chain"/>
        <param argument="--interrupt" name="interrupt" type="boolean" truevalue="--interrupt" falsevalue="" checked="false" label="Allow interrupted folding trajectories when the barrier is exceeded"/>
        <conditional name="barrier_heuristic">
            <param argument="--barrier_heuristic" name="used" type="select" label="Barrier Heuristic">
                <option value="M" selected="true">Morgan-Higgs</option>
                <option value="S">Limit small stacks</option>
                <option value="B">Barriers</option>
                <option value="A">All</option>
            </param>
            <when value="M">
                <param argument="--grouping" name="grouping" type="select" label="How to treat conflict groups">
                    <option value="standard" selected="true">Standard</option>
                    <option value="regroup">Re-group</option>
                </param>
                <param argument="--lookahead" name="lookahead" type="integer" value="1" label="Number of base pairs that MorganHiggs forms its subpaths from"/>
            </when>
            <when value="S"/>
            <when value="B">
                <param argument="--maxkeep" name="maxkeep" type="integer" value="1" label="Breadth of breadth-first search"/>
            </when>
            <when value="A"/>
        </conditional>
        <param argument="--dangle" name="dangle" type="select" label="Dangle value">
            <option value="0" selected="true">0</option>
            <option value="1">1</option>
            <option value="2">2</option>
        </param>
        <param argument="--nolonely" name="noLonelyPairs" type="integer" value="2" label="Value of noLonelyPairs"/>
        <param argument="--transcribed" name="transcribed" type="integer" value="1" label="Number of bases initially transcribed" help="0 means all are transcribed"/>
        <param argument="--transcription_rate" name="transcription_rate" type="float" value="200" label="Number of bases transcribed per second"/>
        <param argument="--windowsize" name="windowsize" type="integer" value="0" label="Max size of substructures considered for folding events during transcription" help="0 = all are considered"/>
    </inputs>
    <outputs>
        <data format="fasta" name="mfe_struct" label="MFE structure from ${tool.name} on ${on_string}"/>
        <data format="tabular" name="trajectory" label="Trajectory of ${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <param name="input_sequence" value="test_sequence_input.fasta"/>
            <param name="init_structure" value="false"/>
            <param name="interrupt" value="false"/>
            <conditional name="barrier_heuristic">
                <param name="used" value="M"/>
                <param name="grouping" value="standard"/>
                <param name="lookahead" value="1"/>
            </conditional>
            <param name="dangle" value="0"/>
            <param name="noLonelyPairs" value="2"/>
            <param name="transcribed" value="1"/>
            <param name="transcription_rate" value="200"/>
            <param name="windowsize" value="0"/>
            <output name="mfe_struct" file="mfe_struct_result.fasta"/>
            <output name="trajectory" file="trajectory_result.tabular"/>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**Kinwalker**

Kinwalker splits the folding process into a series of events where each event can either be a folding event or a transcription event. In each transcription event one base from the RNA sequence is appended to the already transcribed and (partially) folded subsequence.

Kinwalker executes transcription events at regular time intervals. In each folding event a subsequence of the already transcribed RNA sequence is selected and a new structure is formed by combining base pairs from the current structure with base pairs from the mfe structure of that subsequence.

This is done in such a way that the new structure includes base pairs from both structures in an energetically favorable manner. Kinwalker estimates the waiting times for individual folding events depending on the height of the energy barrier between the current structure and the new structure. Folding events between structures can only occur if the energy barrier between them is less than the maximum allowed energy barrier.

As folding paths can only be calculated exhaustively for short sequences, heuristic approaches have to be employed which explicitly construct a (re)folding path between the two structures. The best known algorithm for approximating saddle heights between RNA conformations is the Morgan-Higgs heuristic.

-----

**Input format**

A single RNA sequence in FASTA format.

-----

**Outputs**

- MFE structure in FASTA format
- Folding trajectory in tabular format
]]>
    </help>
    <citations>
        <citation type="doi">10.1016/j.jmb.2008.02.064</citation>
    </citations>
</tool>
