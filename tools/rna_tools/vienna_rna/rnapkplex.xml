<tool id="viennarna_rnapkplex" name="RNAPKplex" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Predict RNA secondary structures including pseudoknots</description>
    <macros>
        <token name="@EXECUTABLE@">RNAPKplex</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    RNAPKplex < '$input' > '$output'
    -T$temperature
    --energyCutoff=$energycutoff
    #if $varExists('$advancedOptions.nolp')
        --cutoff=$advancedOptions.cutoff
        --subopts=$advancedOptions.suboptimal
        $advancedOptions.verbose
        $advancedOptions.noconversion
        $advancedOptions.nolp
        $advancedOptions.nogu
        $advancedOptions.noclosinggu
        $advancedOptions.notetra
    #end if
]]>
    </command>
    <inputs>
        <param format="fasta" name="input" type="data" label="FASTA file"/>
        <param name="temperature" type="float" value="37.0" label="Temperature [°C]" help="-T"/>
        <param argument="--energyCutoff" name="energycutoff" type="float" value="-8.1" label="Pseudoknots cutoff" help="Pseudoknots with smaller energy gains are rejected."/>
        <conditional name="advancedOptions">
            <param name="advancedSelector" type="select" label="Advanced options">
                <option value="basic">Basic options</option>
                <option value="advanced">Advanced options</option>
            </param>
            <when value="advanced">
                <param argument="--subopts" name="suboptimal" type="float" value="0.0" label="Print suboptimal pseudoknots"/>
                <param argument="--verbose" name="verbose" type="boolean" truevalue="--verbose" falsevalue="" checked="false" label="Verbose mode, more output"/>
                <param argument="--cutoff" name="cutoff" type="float" value="0.01" label="Cutoff probability for base pairs in the dot plot"/>
                <param argument="--noLP" name="nolp" type="boolean" truevalue="" falsevalue="--noLP" checked="true" label="Allow lonely base-pairs"/>
                <param argument="--noGU" name="nogu" type="boolean" truevalue="" falsevalue="--noGU" checked="true" label="Allow GU pairing"/>
                <param argument="--noClosingGU" name="noclosinggu" type="boolean" truevalue="" falsevalue="--noClosingGU" checked="true" label="Allow GU pairing at the ends"/>
                <param argument="--noTetra" name="notetra" type="boolean" truevalue="" falsevalue="--noTetra" checked="true" label="Allow stabilization for loops, hairpins etc."/>
                <param argument="--noconv" name="noconversion" type="boolean" truevalue="" falsevalue="--noconv" checked="true" label="Convert Thymine to Uracil (T -> U)"/>
            </when>
            <when value="basic"/>
        </conditional>
    </inputs>
    <outputs>
        <data format="txt" name="output" label="${tool.name} on ${on_string}"/>
        <collection name="matrix_outputs" type="list" label="${tool.name} structure plots on ${on_string}">
            <discover_datasets pattern="(?P&lt;designation&gt;.+)\.ps" ext="eps"/>
        </collection>
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <param name="input" value="rnapkplex_input1.fa"/>
            <output name="output" file="rnapkplex_result1.txt"/>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNAPKplex**

Computes RNA secondary structures by first making two sequence intervals accessible and unpaired using the algorithm of RNAplfold and then calculating the energy of the interaction of those two intervals. The algorithm uses O(n^2*w^4) CPU time and O(n*w^2) memory space. The algorithm furthermore always considers dangle=2 model.

It also produces a PostScript file with a plot of the pseudoknot-free secondary structure graph, in which the bases forming the pseudoknot are marked red.

-----

**Input format**

RNAPKplex requires one FASTA file. The input format is similar to FASTA except that even long sequences may not be interrupted by line breaks, and the header lines are optional.

-----

**Outputs**

- Secondary structure in dot-bracket format
- Images of the calculated structures
]]>
    </help>
    <expand macro="citations"/>
</tool>
