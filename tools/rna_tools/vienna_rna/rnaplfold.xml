<tool id="viennarna_rnaplfold" name="RNAplfold" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Calculate average pair probabilities over sliding windows</description>
    <macros>
        <token name="@EXECUTABLE@">RNAplfold</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    RNAplfold < '$input'
    -T$temperature
    --dangles=$dangling
    --cutoff=$cutoff
    --winsize=$winsize
    --span=$span
    $onthefly
    $openingenergies
    #if $varExists('$unpairedOption.ulength')
        --ulength=$unpairedOption.ulength
    #end if
    #if $varExists('$advancedOptions.nolp')
        $advancedOptions.noconversion
        $advancedOptions.nolp
        $advancedOptions.nogu
        $advancedOptions.noclosinggu
        $advancedOptions.notetra
    #end if
    && find . -name '*_basepairs' -or -name '*_lunp' -or -name '*_openen*' > files.tmp
    && tar -cf '$outputf' --files-from=files.tmp
]]>
    </command>
    <inputs>
        <param format="fasta" name="input" type="data" label="FASTA file"/>
        <param name="temperature" type="float" value="37.0" label="Temperature [°C]" help="-T"/>
        <param name="dangling" type="select" label="How to treat dangling end energies" help="-d">
            <option value="2" selected="true">unpaired bases participate in all dangling ends (2)</option>
            <option value="0">ignore dangling ends (0)</option>
            <option value="1">unpaired bases participate in one dangling end only (1)</option>
            <option value="3">allow coaxial stacking (3)</option>
        </param>
        <param argument="--winsize" name="winsize" type="integer" value="70" label="Average pairing probabilities over this window size"/>
        <param argument="--span" name="span" type="integer" value="70" label="Maximum separation between base pairs"/>
        <param argument="--cutoff" name="cutoff" type="float" value="0.01" label="Cutoff probability for report on base pairing"/>
        <param argument="--print_onthefly" name="onthefly" type="boolean" truevalue="--print_onthefly" falsevalue="" checked="false" label="Print simplified base pair probabilities (_basepairs output)"/>
        <param argument="--opening_energies" name="openingenergies" type="boolean" truevalue="--opening_energies" falsevalue="" checked="false" label="Output in logarithm of the probabilities (_openen output)"/>
        <conditional name="unpairedOption">
            <param name="unpairedSelector" type="select" label="Compute probability that region is unpaired (_lunp output)">
                <option value="no" selected="true">No</option>
                <option value="yes">Yes</option>
            </param>
            <when value="yes">
                <param argument="--ulength" name="ulength" type="integer" value="31" label="Maximal length of unpaired region"/>
            </when>
            <when value="no"/>
        </conditional>
        <conditional name="advancedOptions">
            <param name="advancedSelector" type="select" label="Advanced options">
                <option value="basic">Basic options</option>
                <option value="advanced">Advanced options</option>
            </param>
            <when value="advanced">
                <param argument="--noLP" name="nolp" type="boolean" truevalue="" falsevalue="--noLP" checked="true" label="Allow lonely base-pairs"/>
                <param argument="--noGU" name="nogu" type="boolean" truevalue="" falsevalue="--noGU" checked="true" label="Allow GU pairing"/>
                <param argument="--noClosingGU" name="noclosinggu" type="boolean" truevalue="" falsevalue="--noClosingGU" checked="true" label="Allow GU pairing at the ends"/>
                <param argument="--noTetra" name="notetra" type="boolean" truevalue="" falsevalue="--noTetra" checked="true" label="Allow stabilization for loops, hairpins etc."/>
                <param argument="--noconv" name="noconversion" type="boolean" truevalue="" falsevalue="--noconv" checked="true" label="Convert Thymine to Uracil (T -> U)"/>
            </when>
            <when value="basic"/>
        </conditional>
    </inputs>
    <outputs>
        <collection name="matrix_outputs" type="list" label="${tool.name} dot plots on ${on_string}">
            <discover_datasets pattern="(?P&lt;designation&gt;.+)_dp\.ps" ext="rna_eps" visible="true"/>
        </collection>
        <data format="tar" name="outputf" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <param name="input" value="rnaplfold_input1.fa"/>
            <output_collection name="matrix_outputs" type="list">
                <element name="Anolis_caro_chrUn_GL343590.trna2-A">
                    <assert_contents>
                        <has_text_matching expression="%%Creator: ViennaRNA-@TOOL_VERSION@"/>
                    </assert_contents>
                </element>
            </output_collection>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNAplfold**

Computes local pair probabilities for base pairs with a maximal span of L. The probabilities are averaged over all windows of size L that contain the base pair. For a sequence of length n and a window size of L the algorithm uses only O(n+L*L) memory and O(n*L*L) CPU time. Thus it is practical to scan very large genomes for short stable RNA structures.

-----

**Input format**

RNAplfold requires one FASTA file.

-----

**Outputs**

- Dot plot matrices of pairing probabilities in PostScript format
- A tar file containing _basepairs, _lunp, and _openen outputs (depending on selected options)
]]>
    </help>
    <expand macro="citations"/>
</tool>
