<tool id="viennarna_rnaplot" name="RNAplot" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Draw RNA secondary structures</description>
    <macros>
        <token name="@EXECUTABLE@">RNAplot</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    RNAplot < '$input'
    --layout-type=$layoutSelector
    --output-format=$formatOptions.formatSelector
    #if str($formatOptions.formatSelector) == "eps"
        #if $formatOptions.pre
            --pre='$formatOptions.pre'
        #end if
        #if $formatOptions.post
            --post='$formatOptions.post'
        #end if
        && tar -cf '$imagesFile' *.ps
    #elif str($formatOptions.formatSelector) == "gml"
        && tar -cf '$imagesFile' *.gml
    #elif str($formatOptions.formatSelector) == "svg"
        && tar -cf '$imagesFile' *.svg
    #else
        && tar -cf '$imagesFile' *.ss
    #end if
]]>
    </command>
    <inputs>
        <param format="txt,dbn" name="input" type="data" label="DotBracket structure file"/>
        <param argument="--layout-type" name="layoutSelector" type="select" label="Layout selection">
            <option value="0">Radial Layout</option>
            <option value="1" selected="true">Naview Layout</option>
        </param>
        <conditional name="formatOptions">
            <param argument="--output-format" name="formatSelector" type="select" label="Output format selection">
                <option value="eps" selected="true">PostScript/EPS (.ps)</option>
                <option value="gml">Graph Meta Language (.gml)</option>
                <option value="svg">Scalable Vector Graphics (.svg)</option>
                <option value="xrna">XRNA Save File (.ss)</option>
            </param>
            <when value="eps">
                <param argument="--pre" name="pre" type="text" value="" optional="true" label="Add annotation macros to PostScript file"/>
                <param argument="--post" name="post" type="text" value="" optional="true" label="Add annotations to PostScript file"/>
            </when>
            <when value="gml"/>
            <when value="svg"/>
            <when value="xrna"/>
        </conditional>
    </inputs>
    <outputs>
        <data format="tar" name="imagesFile" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input" value="rnaplot_input1.dbn"/>
            <output name="imagesFile">
                <assert_contents>
                    <has_line line="%%Title: RNA Secondary Structure Plot"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNAplot**

Reads RNA sequences and structures in the format as produced by RNAfold and produces drawings of the secondary structure graph. The coordinates are produced using either E. Bruccoleri's naview routines, or a simple radial layout method.

-----

**Input format**

RNAplot requires one input file in dot-bracket notation (as produced by RNAfold).

-----

**Outputs**

- Structure images bundled together in a tar file (PostScript, SVG, GML, or XRNA format)
]]>
    </help>
    <expand macro="citations"/>
</tool>
