<tool id="viennarna_rnaduplex" name="RNAduplex" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Compute the structure upon hybridization of two RNA strands</description>
    <macros>
        <token name="@EXECUTABLE@">RNAduplex</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
        RNAduplex < '$input' > '$tabularFile'
        -T$temperature -d$dangling --deltaEnergy=$denergy
        #if $varExists('$advancedOptions.noconversion')
            $advancedOptions.noconversion
            $advancedOptions.noLP
            $advancedOptions.noGU
            $advancedOptions.noClosingGU
            $advancedOptions.noTetra
            #if $advancedOptions.nsp
                --nsp='$advancedOptions.nsp'
            #end if
        #end if
]]>
    </command>
    <inputs>
        <param format="fasta" name="input" type="data" label="FASTA file"/>
        <param name="temperature" type="float" value="37.0" label="Temperature [°C]" help="-T"/>
        <param name="dangling" type="select" label="How to treat dangling end energies" help="-d">
            <option value="0">0: ignore dangling ends</option>
            <option value="1">1: unpaired bases participate in one dangling end only</option>
            <option value="2" selected="true">2: unpaired bases participate in all dangling ends</option>
            <option value="3">3: allow coaxial stacking</option>
        </param>
        <param argument="--deltaEnergy" name="denergy" type="float" value="0.0" label="Energy range for suboptimal structures [kcal/mol]"/>
        <param argument="--sorted" type="boolean" truevalue="--sorted" falsevalue="" checked="false" label="Sort output by energy"/>
        <conditional name="advancedOptions">
            <param name="advancedSelector" type="select" label="Advanced options">
                <option value="basic">Basic options</option>
                <option value="advanced">Advanced options</option>
            </param>
            <when value="advanced">
                <param argument="--noconv" name="noconversion" type="boolean" truevalue="" falsevalue="--noconv" checked="true" label="Convert Thymine to Uracil (T -> U)"/>
                <param argument="--noLP" type="boolean" truevalue="" falsevalue="--noLP" checked="true" label="Allow lonely base-pairs"/>
                <param argument="--noGU" type="boolean" truevalue="" falsevalue="--noGU" checked="true" label="Allow GU pairing"/>
                <param argument="--noClosingGU" type="boolean" truevalue="" falsevalue="--noClosingGU" checked="true" label="Allow GU pairing at the ends"/>
                <param argument="--noTetra" type="boolean" truevalue="" falsevalue="--noTetra" checked="true" label="Allow stabilization for loops, hairpins etc."/>
                <param argument="--nsp" type="text" value="" label="Allow non-standard pairs in addition to AU, GC, and GU pairs."
                    help="Comma-separated list of additionally allowed pairs."/>
            </when>
            <when value="basic"/>
        </conditional>
    </inputs>
    <outputs>
        <data format="txt" name="tabularFile" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input" value="rnaduplex_input1.fa"/>
            <output name="tabularFile" file="rnaduplex_result1.txt"/>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNAduplex**

Reads two RNA sequences and computes optimal and suboptimal secondary structures for their hybridization. The calculation is simplified by allowing only inter-molecular base pairs; for the general case use RNAcofold. Suboptimal structures are computed within an energy range of the optimum (kcal/mol).

-----

**Input format**

RNAduplex requires one FASTA file. Structures are calculated pairwise: 1st sequence with 2nd, 3rd with 4th, etc.

-----

**Outputs**

Each line: structure in dot-bracket format with '&' separating the two strands, the range in both sequences as "from,to : from,to", and the energy of the duplex structure in kcal/mol.
]]>
    </help>
    <expand macro="citations"/>
</tool>
