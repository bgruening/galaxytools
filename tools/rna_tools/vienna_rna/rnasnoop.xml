<tool id="viennarna_rnasnoop" name="RNAsnoop" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Find targets of a query H/ACA snoRNA</description>
    <macros>
        <token name="@EXECUTABLE@">RNAsnoop</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    RNAsnoop --query='$filetypeOptions.input1' --target='$filetypeOptions.input2' > '$output'
    --alignmentLength=$length
    $constraint
    --energy-threshold=$energyThreshold
    #if $varExists('$advancedOptions.extensioncost')
        $advancedOptions.fast
        --extension-cost=$advancedOptions.extensioncost
        --minimal-right-duplex=$advancedOptions.minrightduplex
        --minimal-loop-energy=$advancedOptions.minloop
        --minimal-left-duplex=$advancedOptions.minleftduplex
        --minimal-duplex=$advancedOptions.minduplex
        --duplex-distance=$advancedOptions.duplexdist
        --minimal-stem-length=$advancedOptions.minstemlength
        --maximal-stem-length=$advancedOptions.maxstemlength
        --minimal-duplex-box-length=$advancedOptions.minbox
        --maximal-duplex-box-length=$advancedOptions.maxbox
        --minimal-snoRNA-stem-loop-length=$advancedOptions.minsnornastem
        --maximal-snoRNA-stem-loop-length=$advancedOptions.maxsnornastem
        --minimal-snoRNA-duplex-length=$advancedOptions.minsnornaduplex
        --maximal-snoRNA-duplex-length=$advancedOptions.maxsnornaduplex
        --minimal-duplex-stem-energy=$advancedOptions.minduplexstem
        --minimal-total-energy=$advancedOptions.mintotal
        --maximal-stem-asymmetry=$advancedOptions.maxstemasymmetry
        --minimal-lower-stem-energy=$advancedOptions.minstemenergy
    #end if
    #if $filetypeOptions.filetypeSelector == "fasta"
        && tar -cf '$imagesFile' *.ps
    #else
        --alignment-mode
    #end if
]]>
    </command>
    <inputs>
        <conditional name="filetypeOptions">
            <param name="filetypeSelector" type="select" label="Select file type">
                <option value="fasta" selected="true">FASTA</option>
                <option value="clustal">Clustal</option>
            </param>
            <when value="fasta">
                <param format="fasta" name="input1" type="data" label="Query sequences"/>
                <param format="fasta" name="input2" type="data" label="Target sequences"/>
            </when>
            <when value="clustal">
                <param format="txt" name="input1" type="data" label="Query sequences"/>
                <param format="txt" name="input2" type="data" label="Target sequences"/>
            </when>
        </conditional>
        <param argument="--alignmentLength" name="length" type="integer" min="0" value="25" label="Maximal alignment length"/>
        <param argument="--constraint" name="constraint" type="boolean" truevalue="--constraint" falsevalue="" checked="false" label="Calculate the stem structure subject to constraints"/>
        <param argument="--energy-threshold" name="energyThreshold" type="float" value="-1.0" label="Maximal energy difference between the mfe and the desired suboptimal structure"/>
        <conditional name="advancedOptions">
            <param name="advancedSelector" type="select" label="Advanced options">
                <option value="basic">Basic options</option>
                <option value="advanced">Advanced options</option>
            </param>
            <when value="advanced">
                <param argument="--fast-folding" name="fast" type="boolean" truevalue="--fast-folding=1" falsevalue="--fast-folding=0" checked="true" label="Backtracking based on the standard energy model"/>
                <param argument="--extension-cost" name="extensioncost" type="integer" value="0" label="Cost to add each nucleotide in a duplex"/>
                <param argument="--minimal-right-duplex" name="minrightduplex" type="integer" value="-270" label="Minimal right duplex energy"/>
                <param argument="--minimal-loop-energy" name="minloop" type="integer" value="-280" label="Minimal Loop energy"/>
                <param argument="--minimal-left-duplex" name="minleftduplex" type="integer" value="-170" label="Minimal left duplex energy"/>
                <param argument="--minimal-duplex" name="minduplex" type="integer" value="-1090" label="Minimal duplex energy"/>
                <param argument="--duplex-distance" name="duplexdist" type="integer" value="2" label="Distance between target 3' ends of two consecutive duplexes"/>
                <param argument="--minimal-stem-length" name="minstemlength" type="integer" value="5" label="Minimal snoRNA stem length"/>
                <param argument="--maximal-stem-length" name="maxstemlength" type="integer" value="120" label="Maximal snoRNA stem length"/>
                <param argument="--minimal-duplex-box-length" name="minbox" type="integer" value="11" label="Minimal distance between the duplex end and the H/ACA box"/>
                <param argument="--maximal-duplex-box-length" name="maxbox" type="integer" value="16" label="Maximal distance between the duplex end and the H/ACA box"/>
                <param argument="--minimal-snoRNA-stem-loop-length" name="minsnornastem" type="integer" value="1" label="Minimal distance between the stem loop and the snoRNA sequence"/>
                <param argument="--maximal-snoRNA-stem-loop-length" name="maxsnornastem" type="integer" value="100000" label="Maximal distance between the stem loop and the snoRNA sequence"/>
                <param argument="--minimal-snoRNA-duplex-length" name="minsnornaduplex" type="integer" value="0" label="Minimal distance between the duplex and the snoRNA sequence"/>
                <param argument="--maximal-snoRNA-duplex-length" name="maxsnornaduplex" type="integer" value="0" label="Maximal distance between the duplex and the snoRNA sequence"/>
                <param argument="--minimal-duplex-stem-energy" name="minduplexstem" type="integer" value="-1370" label="Minimal duplex stem energy"/>
                <param argument="--minimal-total-energy" name="mintotal" type="integer" value="100000" label="Minimal total energy"/>
                <param argument="--maximal-stem-asymmetry" name="maxstemasymmetry" type="integer" value="30" label="Maximal snoRNA stem asymmetry"/>
                <param argument="--minimal-lower-stem-energy" name="minstemenergy" type="integer" value="100000" label="Minimal lower stem energy"/>
            </when>
            <when value="basic"/>
        </conditional>
    </inputs>
    <outputs>
        <data format="txt" name="output" label="${tool.name} on ${on_string}"/>
        <data format="tar" name="imagesFile" label="${tool.name} images on ${on_string}">
            <filter>filetypeOptions['filetypeSelector'] == "fasta"</filter>
        </data>
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <conditional name="filetypeOptions">
                <param name="filetypeSelector" value="fasta"/>
                <param name="input1" value="rnasnoop_input1b.fa"/>
                <param name="input2" value="rnasnoop_input1a.fa"/>
            </conditional>
            <output name="output" file="rnasnoop_result1.txt"/>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNAsnoop**

Reads a target RNA sequence and a H/ACA snoRNA sequence from a target and query file, respectively, and computes optimal and suboptimal secondary structures for their hybridization. The calculation can be done roughly in O(nm), where n is the length of the target sequence and m is the length of the snoRNA stem, as it is specially tailored to H/ACA snoRNA. For general purpose target predictions, please use RNAduplex, RNAup, RNAcofold, or RNAplex.

-----

**Input format**

RNAsnoop requires two input files:

- Either two FASTA files
- Or two Clustal alignment files

-----

**Outputs**

- Optimal and suboptimal hybridization structures, one per line. Each line contains the structure in dot-bracket format with a "&" separating the two strands, the range in both sequences, and the energy in kcal/mol.
- For FASTA input: PostScript structure plots bundled in a tar file
]]>
    </help>
    <expand macro="citations"/>
</tool>
