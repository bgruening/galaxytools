<tool id="viennarna_rnainverse" name="RNAinverse" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Calculate RNA sequence from secondary structure</description>
    <macros>
        <token name="@EXECUTABLE@">RNAinverse</token>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
    RNAinverse < '$input' > '$outfile'
    --function=#echo ''.join(str($algo).split(','))
    -R$reps
    --final=$final
    #if $varExists('$advancedOptions.nogu')
        $advancedOptions.nogu
        $advancedOptions.noclosinggu
        $advancedOptions.notetra
    #end if
]]>
    </command>
    <inputs>
        <param format="fasta" name="input" type="data" label="Input file (structure in dot-bracket notation, then starting sequence)"/>
        <param name="temperature" type="float" value="37.0" label="Temperature [°C]" help="-T"/>
        <param name="reps" type="integer" value="1" min="1" label="Number of sequences to find that fold close to that structure"
            help="-R"/>
        <param argument="--function" name="algo" type="select" multiple="true" label="Algorithm to use">
            <option value="m" selected="true">Minimum energy</option>
            <option value="p">Partition function</option>
            <validator type="no_options" message="Please select at least one algorithm."/>
        </param>
        <param name="final" type="float" value="0.0" label="Convergence criterion (partition function only)" help="--final"/>
        <param name="dangling" type="select" label="How to treat dangling end energies" help="-d">
            <option value="2" selected="true">unpaired bases participate in all dangling ends (2)</option>
            <option value="0">ignore dangling ends (0)</option>
            <option value="1">unpaired bases participate in one dangling end only (1)</option>
            <option value="3">allow coaxial stacking (3)</option>
        </param>
        <conditional name="advancedOptions">
            <param name="advancedSelector" type="select" label="Advanced options">
                <option value="basic">Basic options</option>
                <option value="advanced">Advanced options</option>
            </param>
            <when value="advanced">
                <param argument="--noGU" name="nogu" type="boolean" truevalue="" falsevalue="--noGU" checked="true" label="Allow GU pairing"/>
                <param argument="--noClosingGU" name="noclosinggu" type="boolean" truevalue="" falsevalue="--noClosingGU" checked="true" label="Allow GU pairing at the ends"/>
                <param argument="--noTetra" name="notetra" type="boolean" truevalue="" falsevalue="--noTetra" checked="true" label="Allow stabilization for loops, hairpins etc."/>
            </when>
            <when value="basic"/>
        </conditional>
    </inputs>
    <outputs>
        <data format="txt" name="outfile" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input" value="rnainverse_input1.clu"/>
            <output name="outfile">
                <assert_contents>
                    <has_line_matching expression="^gggccccnnaauun.*"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**RNAinverse**

Searches for sequences folding into a predefined structure, thereby inverting the folding algorithm. For each search the best sequence found and its Hamming distance to the start sequence are printed. If the search was unsuccessful, a structure distance to the target is appended.

-----

**Input format**

Input is similar to FASTA format. First line: structure in dot-bracket notation; second line: starting sequence. Characters in the start sequence other than "AUGC" are treated as wildcards. Lower case characters are kept fixed during the search.

-----

**Outputs**

- Text output: each sequence with its Hamming distance and optionally the energy if partition function is selected
]]>
    </help>
    <expand macro="citations"/>
</tool>
