<tool id="rbc_rnacommender" name="RNA Commender" version="0.3.0">
    <description>files into a collection</description>
    <requirements>
        <requirement type="package" version="3.5">sam</requirement>
    </requirements>
    <command detect_errors="aggressive">
    <![CDATA[
      $__tool_directory__/rbpfeatures.py asdf asdf asdf "$infile" --out ./temp_out &&
      $__tool_directory__/second.py --in ./temp_out
    ]]></command>
    <inputs>
      <param name="infile" type="data" format="fasta" label="Fasta file to split"/>

    </inputs>
    <outputs>
      <data format="fasta" hidden="false" name="default">
    </outputs>
    <tests>
        <test>
            <param name="inputFile" value="sample1.fasta" />
            <param name="splitmode|splitmode_select" value="each" />
            <output name="default">
              <assert_contents>
                <has_text_matching expression="[ACGU]{19}"/>
              </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
        Takes an input file and writes each consecutive two lines to a separate file, in a dataset collection.
    ]]></help>
    <citations>
        <citation type="bibtex">
            @ARTICLE{bgruening_galaxytools,
                Author = {Björn Grüning, Cameron Smith, Torsten Houwaart, Nicola Soranzo, Eric Rasche},
                keywords = {bioinformatics, ngs, galaxy, cheminformatics, rna},
                title = {{Galaxy Tools - A collection of bioinformatics and cheminformatics tools for the Galaxy environment}},
                url = {https://github.com/bgruening/galaxytools}
            }
        </citation>
    </citations>
</tool>
