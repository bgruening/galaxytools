<tool id="alevin-fry_collate" name="alevin-fry collate" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" python_template_version="3.5" profile="@PROFILE_VERSION@">
    <description>Collate a RAD file by corrected cell barcode</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[
        alevin-fry collate --input-dir '$input1' --rad-dir '$input2'
    ]]></command>
    <inputs>
        <!-- add input-dir -->
        <param type="data" name="file" label="RAD file to be collated" format="rad"/>
    </inputs>
    <outputs>
        <!-- add output -->
    </outputs>
    <tests>
        <test>
            <!-- add tests -->
        </test>
    </tests>
    <!-- add help -->
    <help>
        help
    </help>
    <expand macro="citations"/>
</tool>
