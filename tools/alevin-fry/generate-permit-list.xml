<tool id="alevin-fry_generate-permit-list" name="alevin-fry generate-permit-list" description="Generate a permit list of barcodes from a RAD file" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" python_template_version="3.5" profile="@PROFILE_VERSION@">
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[
        ## add [OPTIONS]
        alevin-fry generate-permit-list --input '$input1' --expected-ori '$EXPECTEDORI' --output-dir '$output1' --expect-cells '$EXPECTCELLS' --force-cells '$FORCECELLS' --valid-bc '$VALIDBC' --min-reads '$MINREADS' --unfiltered-pl '$UNFILTEREDPL'
    ]]></command>
    <inputs>
        <param type="data" name="file" label="map RAD file" format="rad"/>
    </inputs>
    <outputs>
        <!-- add output -->
    </outputs>
    <tests>
        <test>
            <!-- add test -->
        </test>
    </tests>
    <!-- add help -->
    <help>
        help
    </help>
    <expand macro="citations"/>
</tool>
