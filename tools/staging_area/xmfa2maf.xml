<tool id="xmfa2maf" name="XMFA-to-MAF" version="0.0.1">
    <description>converter</description>
    <command>xmfa2maf $input $output</command>
    <inputs>
        <param name="input" type="data" format="xmfa" label="Convert these sequences"/>
    </inputs>
    <outputs>
        <data name="output" format="maf"/>
    </outputs>
    <tests>
    </tests>
    <help>

**What it does**

This tool converts the XMFA multiple alignment format into the MAF format.

    </help>
</tool>
