<tool id="parabricks_fq2bam" name="Parabricks (fq2bam)" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Run GPU-bwa mem to convert FASTQ to BAM</description>
    <macros>
        <token name="@TOOL_VERSION@">4.4.0-1</token>
        <token name="@VERSION_SUFFIX@">0</token>
        <token name="@PROFILE@">24.2</token>
    </macros>
    <creator>
        <organization name="European Galaxy Team" url="https://galaxyproject.org/eu/"/>
        <person givenName="Anup" familyName="Kumar" email="kumara@informatik.uni-freiburg.de"/>
    </creator>
    <requirements>
        <container type="docker">nvcr.io/nvidia/clara/clara-parabricks:@TOOL_VERSION@</container>
    </requirements>
    <command detect_errors="aggressive"><![CDATA[
    #if $reference_source.reference_source_selector == 'history':
        ln -f -s '$reference_source.ref_file' reference.fasta &&
    #else:
        ln -f -s '$reference_source.ref_file.fields.path' reference.fasta &&
    #end if
    #set $o_mappings = 'output_mapped_file.bam'
    ln -f -s '$fastq_input.fastq_input1' input1.fastq &&
    #if $fastq_input.fastq_input_selector == 'paired':
        ln -f -s '$fastq_input.fastq_input2' input2.fastq &&
        pbrun fq2bam --low-memory --ref reference.fasta --in-fq input1.fastq input2.fastq --out-bam '${o_mappings}' &&
    #else:
        pbrun fq2bam --low-memory --ref reference.fasta --in-se-fq input1.fastq --out-bam '${o_mappings}' &&
    #end if   
    cp '${o_mappings}' '$output_mapping'
]]>
    </command>
    <inputs>
        <conditional name="reference_source">
            <param name="reference_source_selector" type="select" label="Will you select a reference genome from your history or use a built-in index?" help="Built-ins were indexed using default options. See `Indexes` section of help below. If you would like to perform self-mapping select `history` here, then choose your input file as reference.">
                <option value="cached">Use a built-in genome index</option>
                <option value="history">Use a genome from history and build index</option>
            </param>
            <when value="cached">
                <param name="ref_file" type="select" label="Using reference genome" help="Select genome from the list">
                    <options from_data_table="all_fasta">
                        <filter type="sort_by" column="2" />
                        <validator type="no_options" message="No reference genomes are available" />
                    </options>
                    <validator type="no_options" message="A built-in reference genome is not available for the build associated with the selected input file"/>
                </param>
            </when>
            <when value="history">
                <param name="ref_file" type="data" format="fasta,fastq" label="Use the following dataset as the reference sequence" help="You can upload a FASTA or FASTQ sequence to the history and use it as reference" />
            </when>
        </conditional>
        <conditional name="fastq_input">
            <param name="fastq_input_selector" type="select" label="Single or Paired-end reads" help="Select between paired and single end data">
                <option value="single">Single</option>
                <option value="paired">Paired</option>
            </param>
            <when value="single">
                <param name="fastq_input1" type="data" format="fastqsanger,fastqsanger.gz,fasta" label="Select fastq dataset" help="Specify dataset with single reads"/>
            </when>
            <when value="paired">
                <param name="fastq_input1" type="data" format="fastqsanger,fastqsanger.gz,fastq,fasta" label="Select first set of reads" help="Specify dataset with forward reads"/>
                <param name="fastq_input2" type="data" format="fastqsanger,fastqsanger.gz,fastq,fasta" label="Select second set of reads" help="Specify dataset with reverse reads"/>
            </when>
        </conditional>
    </inputs>
    <outputs>
        <data format="bam" name="output_mapping" label="${tool.name} on ${on_string} (mapped reads in BAM format)">
            <actions>
                <conditional name="reference_source.reference_source_selector">
                    <when value="cached">
                        <action type="metadata" name="dbkey">
                            <option type="from_data_table" name="all_fasta" column="1" offset="0">
                                <filter type="param_value" column="0" value="#" compare="startswith" keep="False"/>
                                <filter type="param_value" ref="reference_source.ref_file" column="0"/>
                            </option>
                        </action>
                    </when>
                    <when value="history">
                        <action type="metadata" name="dbkey">
                            <option type="from_param" name="reference_source.ref_file" param_attribute="dbkey" />
                        </action>
                    </when>
                </conditional>
            </actions>
        </data>
    </outputs>
    <tests>
        <test>
            <conditional name="reference_source">
                <param name="reference_source_selector" value="cached" />
                <param name="ref_file" value="bwa-mem-mt-genome"/>
            </conditional>
            <conditional name="fastq_input">
                <param name="fastq_input_selector" value="single"/>
                <param name="fastq_input1" ftype="fastqsanger" value="fq2bam-fasta1.fa"/>
            </conditional>
            <output name="output_mapping" ftype="bam" file="parabricks-fq2bam-single.bam" compare="sim_size" />
        </test>
        <test>
            <conditional name="reference_source">
                <param name="reference_source_selector" value="cached" />
                <param name="ref_file" value="bwa-mem-mt-genome"/>
            </conditional>
            <conditional name="fastq_input">
                <param name="fastq_input_selector" value="paired"/>
                <param name="fastq_input1" ftype="fastqsanger" value="fq2bam-fasta1.fa"/>
                <param name="fastq_input2" ftype="fastqsanger" value="fq2bam-fasta2.fa"/>
            </conditional>
            <output name="output_mapping" ftype="bam" file="parabricks-fq2bam-paired.bam" compare="sim_size" />
        </test>
    </tests>
    <help>
        <![CDATA[
**What it does**
 
     Parabricks is a tool for high-performance genomic data analysis, particularly designed for mapping reads to a reference genome. It leverages GPU acceleration to significantly speed up the alignment process.
 
**Input files**
    Single end or paired-end  FASTQ files

**Output file**
    Mapped reads in BAM format


        ]]>
    </help>
    <citations>
        <citation type="bibtex">
            @ARTICLE{nvidia-clara,
                Author = {NVIDIA},
                title = {{Clara Parabricks}},
                url = {https://www.nvidia.com/en-us/clara/genomics/}
            }
        </citation>
    </citations>
</tool>
