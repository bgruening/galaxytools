<tool id="cp-measureobjectintensity" name="MeasureObjectIntensity" version="@CP_VERSION@">
    <description>module of CellProfiler</description>

    <macros><import>macros.xml</import></macros>

    <expand macro="requirements" version="@PY_VERSION@" package="python" />
    <expand macro="stdio" />
    <expand macro="cmd_modules" />

    <configfiles>
        <inputs name="inputs" filename="input.json" />
        <configfile name="script_file">
import json
import sys
import os

FOURSPACES="    "
NEWLINE="\n"

input_json_path = sys.argv[1]
input_pipeline= sys.argv[2]

params = json.load(open(input_json_path, "r"))

def writemoi():
  f.write(NEWLINE)

  f.write("MeasureObjectIntensity:[module_num:"+str(new_count)+"|svn_version:\\'Unknown\\'|variable_revision_number:3|show_window:False|notes:\\x5B\\x5D|batch_state:array(\\x5B\\x5D, dtype=uint8)|enabled:True|wants_pause:False]"+NEWLINE)  

  hidden = str(len(params['rpt_image']))

  f.write(FOURSPACES+"Hidden:" + hidden+NEWLINE)

  for img in params['rpt_image']:
    f.write(FOURSPACES+"Select an image to measure:"+img['image_to_measure']+NEWLINE)

  for obj in params['rpt_object']:
    f.write(FOURSPACES+"Select objects to measure:"+obj['objects_to_measure']+NEWLINE)

  

with open(input_pipeline) as fin:
  lines = fin.readlines()
  
  k,v= lines[4].strip().split(':')

  module_count = int(v)
  new_count = module_count+1
  lines[4]= k+":"+str(new_count)+"\n"
  with open("output","w") as f:
    f.writelines(lines)
    writemoi()

f.close() 
        </configfile>    
    </configfiles>

    <inputs>
        <expand macro="input_pipeline_macro" />
        <repeat name="rpt_image" title="Add another image">
            <param name="image_to_measure" label="Select an image to measure" type="text" />
        </repeat>

        <repeat name="rpt_object" title="Add another object">
            <param name="objects_to_measure" label="Select objects to measure" type="text" />
        </repeat>
    </inputs>

    <outputs>
        <expand macro="output_pipeline_macro" />
    </outputs>

    <tests>
        <test>
            <expand macro="test_input_pipeline_param" />
            <repeat name="rpt_image">
                <param name="image_to_measure" value="DNA"/>
            </repeat>
            <repeat name="rpt_object" >
                <param name="objects_to_measure" value="Nuclei" />
            </repeat>
            <expand macro="test_out_file" file="measure_object_intensity.txt" />
        </test>
    </tests>
    
    <expand macro="help" module="MeasureObjectIntensity" />
    <expand macro="citations" /> 

</tool>

