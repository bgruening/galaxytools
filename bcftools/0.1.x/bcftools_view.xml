<tool id="bcfview" name="bcftoolsView" version="0.1.9">
    <description>Convert, filter, subset VCF/BCF files</description>
    <expand macro="requirements" />
    <expand macro="version_command" />
    <expand macro="stdio" />
    <macros>
        <token name="@EXECUTABLE@">bcftools</token>
        <import>bcftools_macros.xml</import>
    </macros>
    <command>
        @EXECUTABLE@ view $varsitesonly $snpcalling $genotypecalling $bcfinput &gt; $out_file
    </command>
    <inputs>
	<param format="bcf" name="bcfinput" type="data" label="BCF file"/>
        <param name="varsitesonly" type="boolean" truevalue="-v" falsevalue="" checked="false" label="Output Potential Variant Sites Only" help="-v"/>
        <param name="snpcalling" type="boolean" truevalue="-c" falsevalue="" checked="false" label="SNP calling (forces -e)" help="-c"/>
        <param name="genotypecalling" type="boolean" truevalue="-g" falsevalue="" checked="false" label="Call Genotypes at Variant Sites" help="-g"/>

    </inputs>
    <outputs>  
        <data format="vcf" name="out_file" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
    </tests>
    <help>
**BCFTools View**

VCF/BCF conversion, view, subset and filter VCF/BCF files.


**Input format**

bcf files

------

**Outputs**

compressed or uncompressed VCF or BCF files

@CITATION@
    </help>

    <expand macro="citations"/>
</tool>
