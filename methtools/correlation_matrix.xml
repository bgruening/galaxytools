<tool id="r_correlation_matrix" name="Correlation matrix" version="0.1">
  <description></description>
  <requirements>
    <!--<requirement type="package" version="2.3.2">openbabel</requirement>
    <requirement type="package" version="0.1">cheminfolib</requirement>-->
  </requirements>
    <command>Rscript /home/bag/projects/github/galaxytools/methtools/correlation.R
        $infile
        $outfile
        $header

    2>&#38;1

        ##script.py 
        ##  #for $entry in $infiles:
        ##    ${entry.infile} ${entry.column} ${entry.name}
        ##  #end for
  </command>
  <inputs>
        <param name="infile" type="data" format="tabular" label="Select input file" help="Every column is one list of values"/>
        <param name="header" type="boolean" label="Is a header included in the inputfile?" truevalue="TRUE" falsevalue="FALSE" checked="true" />
        <!--
        <repeat name="infiles" title="Input files" min="2">
            <param name="infile" type="data" format="bed, interval" label="Select input file"/>
            <param name="column" default="4" type="data_column" data_ref="infile" label="Column to use for the value"/>
            <param name="name" size="15" type="text" value="" label="Name" help="for plot labeling" />
        </repeat>
        -->
  </inputs>
  <outputs>
        <data format="png" name="outfile" />
  </outputs>
  <tests>

  </tests>
  <help>


.. class:: infomark

**What this tool does**

Plots a correlation matrix. The input should be a matrix, with every column a separate dataset.

  </help>
</tool>
